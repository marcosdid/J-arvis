# F10.6 spec #2 — Manifest Bootstrap Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Quando ▶ Run encontra `<worktree>/.orchestrator/run.yml` faltando, spawn de uma sessão Claude efêmera (sem hooks, sem DB) com um `BOOTSTRAP_PROMPT.md` embedado que orienta Claude a ler o repo e propor o manifest. `fsnotify` detecta o arquivo, valida via `core.LoadManifest`, emite `bootstrap.proposed{manifest_text, valid, errors}` pra UI exibir preview com auto-fire em 10s.

**Architecture:** Novo `BootstrapService` em `internal/core/` — sibling de `RunsService`/`SessionsService`, sem persistência (state em `map[task_id]*bootstrapEntry` protegido por `sync.Mutex`). Watcher goroutine roda single-locked `handleDetection` que reivindica ownership atomicamente, garantindo que `Cancel` vs detecção válida só limpa uma vez. `RunsAPI.Start` muda de `(RunView, error)` para `(StartRunResult{Run?, Bootstrap?}, error)` pra UI distinguir "tem manifest, sobe stack" de "falta manifest, oferece bootstrap". `TasksService` ganha 4ª cleanup chain: bootstrap → sessions → runs → worktrees.

**Tech Stack:** Go 1.26, `github.com/fsnotify/fsnotify` v1.x (nova dep), Wails v2.12, `gopkg.in/yaml.v3` (já tem), TypeScript + React (UI), Vitest + RTL.

**Branch:** `feat/f10.5-catalog-mcp` (continua F10.6 spec #1).

**Reference spec:** `docs/superpowers/specs/2026-05-16-f10.6-bootstrap-design.md`

---

## File map

### Novos arquivos

| Arquivo | Responsabilidade |
|---|---|
| `internal/core/bootstrap.go` | `BootstrapService`, `StartedBootstrap`, `bootstrapEntry`, `BootstrapProposedPayload`, watcher goroutine, `handleDetection`, `Start`, `Cancel`, `CleanupForTask` |
| `internal/core/bootstrap_test.go` | Unit tests (16 casos) |
| `internal/core/bootstrap_prompt.md` | Markdown embedado via `//go:embed bootstrap_prompt.md` |
| `internal/core/bootstrap_integration_test.go` | `//go:build integration` smoke com ai-jail real |
| `internal/api/bootstrap.go` | `BootstrapAPI` Wails-bound; `BootstrapView` (JSON) |
| `internal/api/bootstrap_test.go` | Contrato Wails + mapeamento de erros |
| `ui/src/wailsjs/go/api/BootstrapAPI.{d.ts,js}` | Auto-gerado por `wails generate module` |

### Modificados

| Arquivo | Mudança |
|---|---|
| `go.mod` + `go.sum` | + `github.com/fsnotify/fsnotify v1.7.0+` |
| `internal/core/tasks.go` | + `TasksBootstrapCleanupFn`; + `runBootstrapCleanup`; chama 1º em `Patch` e `Discard` |
| `internal/core/tasks_test.go` | + caso "terminal triggers 4 cleanups in order" |
| `internal/api/runs.go` | `Start` retorna `StartRunResult{Run, Bootstrap}`; `BootstrapHint` novo tipo |
| `internal/api/runs_test.go` | Atualiza shape esperado |
| `main.go` | Constrói `bootstrapSvc` + `bootstrapAPI`; passa `bootstrapSvc.CleanupForTask` ao `NewTasksService`; adiciona ao `Bind` |
| `cmd/jarvis-e2e-http/main.go` | Mesma wiring (atrás de `//go:build e2e_http`) |
| `ui/src/lib/api.ts:296` | `startRun` retorna `StartRunResult` |
| `ui/src/lib/api.ts:316` | `bootstrapManifest` deixa de ser stub `notFound` |
| `ui/src/lib/api.ts` | + `cancelBootstrap(taskID): Promise<void>` |
| `ui/src/lib/events.ts:78` | `bootstrap.proposed` payload ganha `manifest_text`, `valid`, `errors`; `task_id: string` (não mais null) |
| `ui/src/hooks/useRun.ts` | Consome `StartRunResult.bootstrap` |
| `ui/src/hooks/useRun.test.ts:77` | Mock `api.startRun` → `{ run: {...}, bootstrap: null }` |
| `ui/src/hooks/useRun.test.ts:108` | Mock `bootstrapManifest` → shape real (não mais `notFound`) |
| `ui/src/hooks/useSessionEvents.ts:79` | Handler `bootstrap.proposed` despacha pro store/callback |
| `ui/src/hooks/useSessionEvents.test.ts:219` | Update payload no test |
| `ui/src/components/dialogs/BootstrapModal.tsx` | Adiciona fase 2: preview YAML + countdown 10s + [Run agora] [Cancelar] |
| `ui/src/components/dialogs/BootstrapModal.test.tsx` | Casos fase 2 |
| `ui/src/wailsjs/go/api/RunsAPI.{d.ts,js}` | Regenerar (Start retorna StartRunResult) |

---

## Commit convention

Todos os commits prefixam `F10.6.B.<stage>.<step>:` (B = "Bootstrap" — distingue dos `F10.6.R.*` do spec #1). Exemplos:
- `feat(F10.6.B.2.1): BootstrapService types + ErrManifestAlreadyExists`
- `test(F10.6.B.3.1): TestStart_TaskInTerminalState`

---

## Stage 1 — Dependência + prompt embedado

Objetivo: ter `fsnotify` no `go.mod` e `BOOTSTRAP_PROMPT.md` pronto pra embed. Tudo isso é prereq pro `BootstrapService` compilar.

### Task 1.1: Adicionar fsnotify

**Files:**
- Modify: `go.mod`
- Modify: `go.sum`

- [ ] **Step 1: Adicionar dependência**

```bash
go get github.com/fsnotify/fsnotify@latest
```

Expected: `go.mod` ganha linha `require github.com/fsnotify/fsnotify v1.x.x`. `go.sum` ganha entradas correspondentes (e algumas transitive como `golang.org/x/sys`).

- [ ] **Step 2: Sanidade — compila e vet limpo**

```bash
go vet -tags webkit2_41 ./...
```

Expected: sem output (sucesso).

- [ ] **Step 3: Commit**

```bash
git add go.mod go.sum
git commit -m "$(cat <<'EOF'
chore(F10.6.B.1.1): add fsnotify dependency

For the bootstrap watcher in F10.6 spec #2. Event-driven (inotify on
Linux) instead of polling.
EOF
)"
```

### Task 1.2: Escrever BOOTSTRAP_PROMPT.md

**Files:**
- Create: `internal/core/bootstrap_prompt.md`

- [ ] **Step 1: Escrever o markdown**

Conteúdo completo (~80 linhas — orienta Claude sobre quem é Jarvis, o schema do run.yml, checklist de leitura, instrução final):

```markdown
# Bootstrap do manifest J-arvis

Você é uma sessão **efêmera** do Claude rodando dentro de um worktree gerenciado pelo J-arvis. Sua **única tarefa**: ler este repositório e escrever `.orchestrator/run.yml` descrevendo a stack que precisa subir pra rodar o projeto em modo dev.

Quando você salvar o arquivo, o J-arvis detecta automaticamente, valida o YAML e — se válido — sobe a stack via Docker. Você pode fechar o terminal depois disso.

## Schema do `.orchestrator/run.yml`

```yaml
version: "1"
services:
  <service-name>:
    image: <docker image>          # OU "build:" (mutuamente exclusivos)
    build: <path-relativo-ao-cwd>  # se "build", aponta pra dir com Dockerfile
    port: <port-do-container>      # opcional; só pra expor + substituições
    depends_on:                    # opcional; ordem de start
      - <outro-service>
    env:                           # opcional; values podem usar tokens
      DATABASE_URL: "postgres://app:app@$URL_db/app"
      API_PORT: "$PORT_api"
      RUN_ID: "$RUN_ID"
      WORKSPACE: "$CWD"
    healthcheck:                   # opcional; espera ficar healthy antes do depends_on
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 5s
      timeout: 3s
      retries: 6
    seed:                          # opcional; roda 1x após healthcheck
      command: ["./scripts/seed.sh"]
      timeout: 60s
    mount_source: true             # opcional; bind-mount o worktree em /workspace
```

### Tokens de substituição em `env:`

- `$PORT_<svc>` → porta host alocada dinamicamente pro serviço `<svc>`
- `$URL_<svc>` → `http://localhost:<porta-host-do-svc>`
- `$RUN_ID` → UUID da run atual (útil pra namespacing)
- `$CWD` → caminho absoluto do worktree

### Regras de validação que o J-arvis aplica

- `version: "1"` obrigatório
- Cada serviço deve ter **OU** `image` **OU** `build`, nunca os dois
- `depends_on` não pode formar ciclos
- Tokens `$PORT_<svc>` / `$URL_<svc>` precisam referenciar serviço existente que tenha `port:` declarado

## O que você deve fazer

1. **Leia o repositório.** Cheque na ordem:
   - `README.md` (instruções de "como rodar localmente")
   - `Dockerfile`, `docker-compose.yml`, `docker-compose.dev.yml` (se existirem — copie a estrutura, simplificando pra dev)
   - `package.json` (Node — scripts `dev`/`start`, deps de DB)
   - `pyproject.toml` / `requirements.txt` (Python — FastAPI/Django/Flask?)
   - `go.mod` (Go — entrypoint em `cmd/`?)
   - `.env.example` (variáveis necessárias)
   - `Makefile` (target `dev`?)

2. **Decida a stack mínima pra dev:** geralmente 1 web/api + 1 DB. Não inclua coisas opcionais (cache externo, observability) a menos que sejam **estritamente necessárias** pro app subir.

3. **Escreva `.orchestrator/run.yml`** seguindo o schema acima. Prefira:
   - `image:` pra DBs e serviços bem conhecidos (`postgres:16`, `redis:7-alpine`)
   - `build: .` pra o app principal (assume Dockerfile na raiz)
   - `mount_source: true` no app principal (hot-reload sem rebuild)

4. **Não execute nada além de leitura e Write.** Não rode `docker`, `npm install`, `make`, etc. O J-arvis cuida da execução depois.

5. **Quando terminar**, simplesmente salve o arquivo. O daemon detecta em milissegundos e te avisa via fechamento do terminal (você pode pedir pra fechar manualmente).

## Exemplo (Node + Postgres)

```yaml
version: "1"
services:
  db:
    image: postgres:16
    port: 5432
    env:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_DB: app
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app"]
      interval: 3s
      timeout: 2s
      retries: 10
  api:
    build: .
    port: 3000
    depends_on:
      - db
    env:
      DATABASE_URL: "postgres://app:app@$URL_db/app"
      PORT: "3000"
    mount_source: true
```

Boa! Comece pelo README.
```

- [ ] **Step 2: Commit**

```bash
git add internal/core/bootstrap_prompt.md
git commit -m "$(cat <<'EOF'
docs(F10.6.B.1.2): embedded BOOTSTRAP_PROMPT.md template

Markdown loaded into the bootstrap Claude session via //go:embed. Lays
out the manifest schema, validation rules, a checklist of files to
read, and a worked example. The session has no MCP/hooks — this file
is its only on-disk instruction.
EOF
)"
```

### Task 1.3: Verificar embed (smoke)

**Files:**
- Test: `internal/core/bootstrap_test.go` (smoke parcial só pra validar `//go:embed`)

- [ ] **Step 1: Escrever stub mínimo do service só pro embed**

Cria `internal/core/bootstrap.go` com APENAS o embed (sem service ainda):

```go
package core

import _ "embed"

//go:embed bootstrap_prompt.md
var bootstrapPromptTemplate string
```

- [ ] **Step 2: Escrever test do embed**

`internal/core/bootstrap_test.go`:

```go
package core

import (
	"strings"
	"testing"
)

func TestBootstrapPromptEmbedded(t *testing.T) {
	if len(bootstrapPromptTemplate) < 500 {
		t.Fatalf("bootstrap prompt seems empty or truncated: %d bytes", len(bootstrapPromptTemplate))
	}
	if !strings.Contains(bootstrapPromptTemplate, "version: \"1\"") {
		t.Error("prompt missing version: \"1\" example")
	}
	if !strings.Contains(bootstrapPromptTemplate, ".orchestrator/run.yml") {
		t.Error("prompt missing target path reference")
	}
}
```

- [ ] **Step 3: Roda**

```bash
go test -tags webkit2_41 -run TestBootstrapPromptEmbedded ./internal/core/...
```

Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add internal/core/bootstrap.go internal/core/bootstrap_test.go
git commit -m "$(cat <<'EOF'
feat(F10.6.B.1.3): //go:embed bootstrap_prompt.md into core

Stub bootstrap.go (just the embed); test asserts the markdown is loaded
and contains the schema example. Real BootstrapService lands in Stage 2+.
EOF
)"
```

---

## Stage 2 — Types e constructor

Objetivo: definir todos os types (`StartedBootstrap`, `bootstrapEntry`, `BootstrapProposedPayload`) + `NewBootstrapService` vazio. Compila mas não faz nada útil ainda.

### Task 2.1: Types + erros

**Files:**
- Modify: `internal/core/bootstrap.go`
- Modify: `internal/core/errors.go` (adiciona `ErrManifestAlreadyExists`)

- [ ] **Step 1: Adicionar `ErrManifestAlreadyExists`**

Append em `internal/core/errors.go`:

```go
var ErrManifestAlreadyExists = errors.New("bootstrap: .orchestrator/run.yml already exists")
```

- [ ] **Step 2: Expandir `bootstrap.go` com types**

Substitui o conteúdo atual de `internal/core/bootstrap.go` por:

```go
package core

import (
	"context"
	_ "embed"
	"sync"
	"time"

	"github.com/fsnotify/fsnotify"

	"github.com/marcosdid/jarvis/internal/catalog"
	"github.com/marcosdid/jarvis/internal/events"
	"github.com/marcosdid/jarvis/internal/sandbox"
	"github.com/marcosdid/jarvis/internal/store"
)

//go:embed bootstrap_prompt.md
var bootstrapPromptTemplate string

// StartedBootstrap is the Go-internal return type from BootstrapService.Start.
// The WatcherReady channel is closed by the watcher goroutine just before its
// first select iteration — tests use it as a sync barrier to avoid losing
// fsnotify events fired before the watcher began consuming the Events channel.
// The Wails-facing api.BootstrapView strips this field for JSON.
type StartedBootstrap struct {
	SessionID    string
	Cwd          string
	ManifestPath string
	PromptPath   string
	WatcherReady <-chan struct{}
}

// BootstrapProposedPayload is emitted on the "bootstrap.proposed" event when
// the watcher detects .orchestrator/run.yml (valid or not). JSON tags are
// explicit — events.LazyEmitter goes through json.Marshal and field names
// without tags would CamelCase and break the TypeScript payload type.
type BootstrapProposedPayload struct {
	TaskID       string   `json:"task_id"`
	SessionID    string   `json:"session_id"`
	ManifestText string   `json:"manifest_text"`
	Valid        bool     `json:"valid"`
	Errors       []string `json:"errors"`
}

type bootstrapEntry struct {
	sessionID    string
	cwd          string
	handle       sandbox.Handle
	watcher      *fsnotify.Watcher
	cancel       context.CancelFunc
	watcherReady chan struct{}
	startedAt    time.Time
}

// BootstrapService manages ephemeral Claude sessions whose purpose is to
// write a .orchestrator/run.yml manifest. State lives only in memory — no
// DB row (the bootstrap is conceptually a one-shot wizard, not a tracked
// session).
type BootstrapService struct {
	runtime sandbox.Runtime
	wtSvc   *WorktreesService
	tasks   *store.TasksRepo
	catalog *catalog.Catalog
	bus     events.Emitter

	mu     sync.Mutex
	active map[string]*bootstrapEntry
}

func NewBootstrapService(
	runtime sandbox.Runtime,
	wtSvc *WorktreesService,
	tasks *store.TasksRepo,
	cat *catalog.Catalog,
	bus events.Emitter,
) *BootstrapService {
	return &BootstrapService{
		runtime: runtime, wtSvc: wtSvc, tasks: tasks, catalog: cat, bus: bus,
		active: make(map[string]*bootstrapEntry),
	}
}
```

- [ ] **Step 3: Verifica compila + embed test passa**

```bash
go vet -tags webkit2_41 ./internal/core/...
go test -tags webkit2_41 -run TestBootstrapPromptEmbedded ./internal/core/...
```

Expected: vet sem output. Test PASS.

- [ ] **Step 4: Commit**

```bash
git add internal/core/bootstrap.go internal/core/errors.go
git commit -m "$(cat <<'EOF'
feat(F10.6.B.2.1): BootstrapService types + ErrManifestAlreadyExists

Defines StartedBootstrap (Go-internal, has WatcherReady channel),
BootstrapProposedPayload (event shape with snake_case JSON tags), and
bootstrapEntry (in-map state per task). Constructor takes the same
sandbox.Runtime seam used by SessionsService — pattern parity.
EOF
)"
```

---

## Stage 3 — `Start` happy path + guards (TDD)

Objetivo: implementar `Start` com todas as guardas (`ErrTaskInTerminalState`, `ErrSandboxUnavailable`, `ErrManifestAlreadyExists`, idempotência) e o caminho feliz (resolve worktree, escreve prompt + .ai-jail, cria watcher, spawn, insere no map, retorna `*StartedBootstrap`).

Não escreve a goroutine do watcher ainda — só inicializa `fsnotify.Watcher` e arma `watcherReady`. Stage 5 acende a goroutine.

### Task 3.1: Test helpers + fakeRuntime reuse

**Files:**
- Modify: `internal/core/bootstrap_test.go`

- [ ] **Step 1: Adicionar helpers no `bootstrap_test.go`**

Append depois do `TestBootstrapPromptEmbedded`:

```go
import (
	"context"
	"path/filepath"
	"os"
	"sync"
	"testing"
	"time"

	"github.com/marcosdid/jarvis/internal/catalog"
	"github.com/marcosdid/jarvis/internal/events"
	"github.com/marcosdid/jarvis/internal/sandbox"
	"github.com/marcosdid/jarvis/internal/store"
)

// bootstrapFakeRuntime: noop spawn that records calls + supports an injected
// error. Mirrors sessions_test.go's fakeRuntime.
type bootstrapFakeRuntime struct {
	mu        sync.Mutex
	spawnErr  error
	spawnArgs []sandbox.RuntimeSpec
	killArgs  []sandbox.Handle
	killErr   error
	nextPID   int
}

func (r *bootstrapFakeRuntime) Spawn(_ context.Context, spec sandbox.RuntimeSpec) (sandbox.Handle, error) {
	r.mu.Lock()
	defer r.mu.Unlock()
	if r.spawnErr != nil {
		return sandbox.Handle{}, r.spawnErr
	}
	r.spawnArgs = append(r.spawnArgs, spec)
	r.nextPID++
	return sandbox.Handle{PID: 10000 + r.nextPID}, nil
}

func (r *bootstrapFakeRuntime) Kill(_ context.Context, h sandbox.Handle) error {
	r.mu.Lock()
	defer r.mu.Unlock()
	r.killArgs = append(r.killArgs, h)
	return r.killErr
}

func (r *bootstrapFakeRuntime) SpawnCount() int {
	r.mu.Lock(); defer r.mu.Unlock()
	return len(r.spawnArgs)
}

func (r *bootstrapFakeRuntime) KillCount() int {
	r.mu.Lock(); defer r.mu.Unlock()
	return len(r.killArgs)
}

// newBootstrapTestEnv builds a BootstrapService backed by a real SQLite DB
// (via store.OpenInMemory + Migrate), a tmp worktree, a real WorktreesService
// using a fake git, and the fake runtime. Returns the service plus convenience
// handles for assertions.
type bootstrapTestEnv struct {
	svc      *BootstrapService
	runtime  *bootstrapFakeRuntime
	emitter  *events.FakeEmitter
	tasksRepo *store.TasksRepo
	taskID   string
	worktree string // tmp dir registered as the worktree
	cleanup  func()
}

func newBootstrapTestEnv(t *testing.T) *bootstrapTestEnv {
	t.Helper()
	ctx := context.Background()
	db, err := store.OpenInMemory(ctx)
	if err != nil { t.Fatalf("OpenInMemory: %v", err) }
	if err := store.Migrate(ctx, db); err != nil { t.Fatalf("Migrate: %v", err) }

	projectsRepo := store.NewProjectsRepo(db)
	reposRepo := store.NewRepositoriesRepo(db)
	wtsRepo := store.NewWorktreesRepo(db)
	tasksRepo := store.NewTasksRepo(db)

	wt := t.TempDir() // simulated worktree dir
	projRoot := t.TempDir()

	// Seed: project with one repo whose worktree is wt.
	proj, err := projectsRepo.Create(ctx, store.CreateProjectInput{
		Name: "p", Path: projRoot,
	})
	if err != nil { t.Fatalf("Create project: %v", err) }
	repo, err := reposRepo.Create(ctx, proj.ID, "repo-a", "")
	if err != nil { t.Fatalf("Create repo: %v", err) }
	task, err := tasksRepo.Create(ctx, store.CreateTaskInput{
		ProjectID: proj.ID, Title: "t", Description: "", State: "in_progress",
	})
	if err != nil { t.Fatalf("Create task: %v", err) }
	if _, err := wtsRepo.Insert(ctx, store.Worktree{
		RepositoryID: repo.ID, TaskID: &task.ID, Path: wt,
	}); err != nil { t.Fatalf("Insert worktree: %v", err) }

	cat := &catalog.Catalog{
		Version: "1",
		FallbackPermissionProfile: "fallback",
		PermissionProfiles: map[string]catalog.PermissionProfile{
			"fallback": {Name: "fallback", ClaudeArgs: []string{"--print"}},
		},
	}
	fake := &events.FakeEmitter{}
	runtime := &bootstrapFakeRuntime{}
	wtSvc := NewWorktreesService(wtsRepo, reposRepo, projectsRepo, &fakeGit{}, fake)

	svc := NewBootstrapService(runtime, wtSvc, tasksRepo, cat, fake)
	return &bootstrapTestEnv{
		svc: svc, runtime: runtime, emitter: fake,
		tasksRepo: tasksRepo, taskID: task.ID, worktree: wt,
		cleanup: func() { _ = db.Close() },
	}
}
```

> **Note:** `fakeGit` already exists in `internal/core/fake_git_test.go` — reuse it. If `store.OpenInMemory` doesn't exist, use `store.Open(ctx, ":memory:")` instead — check `store/db.go` for the correct call.

- [ ] **Step 2: Compila smoke**

```bash
go build -tags webkit2_41 ./internal/core/...
```

Expected: success.

- [ ] **Step 3: Commit (helpers only — sem teste novo ainda)**

```bash
git add internal/core/bootstrap_test.go
git commit -m "$(cat <<'EOF'
test(F10.6.B.3.1): test helpers for BootstrapService

bootstrapFakeRuntime mirrors sessions_test.go's fakeRuntime pattern.
newBootstrapTestEnv builds a real in-memory DB + tmp worktree + seeded
project/repo/task — gives each test a fresh environment with one
existing worktree, ready for Start.
EOF
)"
```

### Task 3.2: TestStart_TaskInTerminalState

- [ ] **Step 1: Write failing test**

Append em `bootstrap_test.go`:

```go
func TestStart_TaskInTerminalState(t *testing.T) {
	env := newBootstrapTestEnv(t)
	defer env.cleanup()
	ctx := context.Background()

	// Move task to terminal state ("done").
	if _, err := env.tasksRepo.UpdateState(ctx, env.taskID, "done"); err != nil {
		t.Fatalf("UpdateState: %v", err)
	}

	_, err := env.svc.Start(ctx, env.taskID)
	if !errors.Is(err, ErrTaskInTerminalState) {
		t.Fatalf("Start: err=%v, want ErrTaskInTerminalState", err)
	}
	if env.runtime.SpawnCount() != 0 {
		t.Errorf("spawn called %d times; want 0", env.runtime.SpawnCount())
	}
}
```

(Adicione `"errors"` ao import se faltar.)

- [ ] **Step 2: Run, verify FAIL**

```bash
go test -tags webkit2_41 -run TestStart_TaskInTerminalState ./internal/core/...
```

Expected: FAIL com "Start undefined" (Service não tem método ainda).

- [ ] **Step 3: Implementar guarda em `bootstrap.go`**

Adicione no fim do `bootstrap.go`:

```go
func (b *BootstrapService) Start(ctx context.Context, taskID string) (*StartedBootstrap, error) {
	task, err := b.tasks.Get(ctx, taskID)
	if err != nil {
		return nil, err
	}
	if IsTerminal(task.State) {
		return nil, ErrTaskInTerminalState
	}
	return nil, errors.New("Start: not implemented yet") // TDD placeholder
}
```

(Imports: `context`, `errors`.)

- [ ] **Step 4: Run, verify PASS**

```bash
go test -tags webkit2_41 -run TestStart_TaskInTerminalState ./internal/core/...
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add internal/core/bootstrap.go internal/core/bootstrap_test.go
git commit -m "$(cat <<'EOF'
feat(F10.6.B.3.2): BootstrapService.Start rejects terminal tasks

Mirrors the SessionsService guard. Test asserts no spawn happens when
the task is already done/abandoned.
EOF
)"
```

### Task 3.3: TestStart_SandboxUnavailable

- [ ] **Step 1: Write failing test**

```go
func TestStart_SandboxUnavailable(t *testing.T) {
	env := newBootstrapTestEnv(t)
	defer env.cleanup()
	// Force preflight failure by pointing PATH to an empty dir (ai-jail not found).
	t.Setenv("PATH", t.TempDir())

	_, err := env.svc.Start(context.Background(), env.taskID)
	if !errors.Is(err, ErrSandboxUnavailable) {
		t.Fatalf("Start: err=%v, want ErrSandboxUnavailable", err)
	}
	if env.runtime.SpawnCount() != 0 {
		t.Errorf("spawn called %d times; want 0", env.runtime.SpawnCount())
	}
}
```

- [ ] **Step 2: Run, verify FAIL**

Expected: FAIL (Start retorna placeholder error, não ErrSandboxUnavailable).

- [ ] **Step 3: Implementar preflight**

Atualize `Start`:

```go
func (b *BootstrapService) Start(ctx context.Context, taskID string) (*StartedBootstrap, error) {
	if err := sandbox.SandboxAvailable(); err != nil {
		return nil, fmt.Errorf("%w: %v", ErrSandboxUnavailable, err)
	}
	if _, err := sandbox.DetectTerminal(); err != nil {
		return nil, fmt.Errorf("%w: %v", ErrSandboxUnavailable, err)
	}

	task, err := b.tasks.Get(ctx, taskID)
	if err != nil {
		return nil, err
	}
	if IsTerminal(task.State) {
		return nil, ErrTaskInTerminalState
	}
	return nil, errors.New("Start: not implemented yet")
}
```

Adicionar `"fmt"` ao import.

- [ ] **Step 4: Run, verify PASS**

```bash
go test -tags webkit2_41 -run TestStart_SandboxUnavailable ./internal/core/...
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add internal/core/bootstrap.go internal/core/bootstrap_test.go
git commit -m "feat(F10.6.B.3.3): Start preflight (sandbox + terminal)"
```

### Task 3.4: TestStart_ManifestAlreadyExists

- [ ] **Step 1: Write failing test**

```go
func TestStart_ManifestAlreadyExists(t *testing.T) {
	env := newBootstrapTestEnv(t)
	defer env.cleanup()

	// Pre-create the manifest file.
	orchDir := filepath.Join(env.worktree, ".orchestrator")
	if err := os.MkdirAll(orchDir, 0o755); err != nil {
		t.Fatalf("mkdir: %v", err)
	}
	if err := os.WriteFile(filepath.Join(orchDir, "run.yml"), []byte("version: \"1\"\n"), 0o644); err != nil {
		t.Fatalf("write: %v", err)
	}

	_, err := env.svc.Start(context.Background(), env.taskID)
	if !errors.Is(err, ErrManifestAlreadyExists) {
		t.Fatalf("Start: err=%v, want ErrManifestAlreadyExists", err)
	}
	if env.runtime.SpawnCount() != 0 {
		t.Errorf("spawn called %d times; want 0", env.runtime.SpawnCount())
	}
}
```

- [ ] **Step 2: Run, FAIL**

- [ ] **Step 3: Implementar — adicionar resolução de worktree + check do manifest**

Substitui o stub `Start` por uma versão que já resolve worktree e verifica manifest:

```go
func (b *BootstrapService) Start(ctx context.Context, taskID string) (*StartedBootstrap, error) {
	if err := sandbox.SandboxAvailable(); err != nil {
		return nil, fmt.Errorf("%w: %v", ErrSandboxUnavailable, err)
	}
	if _, err := sandbox.DetectTerminal(); err != nil {
		return nil, fmt.Errorf("%w: %v", ErrSandboxUnavailable, err)
	}

	task, err := b.tasks.Get(ctx, taskID)
	if err != nil {
		return nil, err
	}
	if IsTerminal(task.State) {
		return nil, ErrTaskInTerminalState
	}

	cwd, err := b.resolveCwd(ctx, taskID, task)
	if err != nil {
		return nil, err
	}

	manifestPath := filepath.Join(cwd, ".orchestrator", "run.yml")
	if _, err := os.Stat(manifestPath); err == nil {
		return nil, ErrManifestAlreadyExists
	}

	return nil, errors.New("Start: not implemented yet")
}

// resolveCwd mirrors SessionsService — if no worktree, create one.
func (b *BootstrapService) resolveCwd(ctx context.Context, taskID string, task *store.Task) (string, error) {
	wts, err := b.wtSvc.repo.ListByTask(ctx, taskID)
	if err != nil {
		return "", err
	}
	if len(wts) == 0 {
		branch := taskBranchOrSlug(task)
		wts, err = b.wtSvc.CreateForTask(ctx, taskID, branch)
		if err != nil {
			return "", fmt.Errorf("create worktrees for task: %w", err)
		}
	}
	cwd := wts[0].Path
	if len(wts) > 1 {
		cwd = filepath.Dir(wts[0].Path)
	}
	return cwd, nil
}
```

Imports adicionais: `os`, `path/filepath`.

> **Note:** `b.wtSvc.repo` é um campo privado. Cheque `internal/core/worktrees.go` — pode precisar de getter `ListByTask` no WorktreesService ou acesso direto ao repo via passar `worktreesRepo` separadamente no constructor do BootstrapService. **Recomendado:** seguir o padrão de `SessionsService` que recebe `worktrees *store.WorktreesRepo` separado.

**Refactor (do mesmo step):** muda construtor pra incluir `worktrees *store.WorktreesRepo`:

```go
type BootstrapService struct {
	runtime   sandbox.Runtime
	wtSvc     *WorktreesService
	worktrees *store.WorktreesRepo  // novo
	tasks     *store.TasksRepo
	catalog   *catalog.Catalog
	bus       events.Emitter
	// ...
}

func NewBootstrapService(
	runtime sandbox.Runtime,
	wtSvc *WorktreesService,
	worktrees *store.WorktreesRepo,
	tasks *store.TasksRepo,
	cat *catalog.Catalog,
	bus events.Emitter,
) *BootstrapService { /* ... */ }
```

Atualiza `newBootstrapTestEnv` pra passar `wtsRepo` no `NewBootstrapService`.
Atualiza `resolveCwd` pra usar `b.worktrees.ListByTask`.

- [ ] **Step 4: Run, PASS**

```bash
go test -tags webkit2_41 -run TestStart_ManifestAlreadyExists ./internal/core/...
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add internal/core/bootstrap.go internal/core/bootstrap_test.go
git commit -m "$(cat <<'EOF'
feat(F10.6.B.3.4): Start resolves cwd + rejects when manifest exists

Mirrors SessionsService cwd-resolution (create worktree if missing,
filepath.Dir for multi-repo). New ErrManifestAlreadyExists guards the
UI race where Claude in another window beat the user clicking
"Iniciar bootstrap".

Constructor gained an explicit *store.WorktreesRepo parameter — same
pattern as SessionsService — instead of reaching into wtSvc.repo.
EOF
)"
```

### Task 3.5: TestStart_HappyPath

- [ ] **Step 1: Write failing test**

```go
func TestStart_HappyPath(t *testing.T) {
	env := newBootstrapTestEnv(t)
	defer env.cleanup()

	started, err := env.svc.Start(context.Background(), env.taskID)
	if err != nil {
		t.Fatalf("Start: %v", err)
	}
	if started == nil {
		t.Fatal("Start returned nil StartedBootstrap")
	}
	if started.SessionID == "" {
		t.Error("SessionID empty")
	}
	if started.Cwd != env.worktree {
		t.Errorf("Cwd=%q, want %q", started.Cwd, env.worktree)
	}
	wantManifest := filepath.Join(env.worktree, ".orchestrator", "run.yml")
	if started.ManifestPath != wantManifest {
		t.Errorf("ManifestPath=%q, want %q", started.ManifestPath, wantManifest)
	}
	wantPrompt := filepath.Join(env.worktree, ".orchestrator", "BOOTSTRAP_PROMPT.md")
	if started.PromptPath != wantPrompt {
		t.Errorf("PromptPath=%q, want %q", started.PromptPath, wantPrompt)
	}
	if started.WatcherReady == nil {
		t.Error("WatcherReady nil")
	}

	// Files on disk
	promptData, err := os.ReadFile(wantPrompt)
	if err != nil {
		t.Fatalf("ReadFile prompt: %v", err)
	}
	if !strings.Contains(string(promptData), ".orchestrator/run.yml") {
		t.Error("prompt missing schema reference")
	}
	if _, err := os.Stat(filepath.Join(env.worktree, ".ai-jail")); err != nil {
		t.Errorf(".ai-jail not written: %v", err)
	}

	// Spawn called once with correct cwd
	if got := env.runtime.SpawnCount(); got != 1 {
		t.Fatalf("spawn count=%d, want 1", got)
	}
}
```

- [ ] **Step 2: Run, FAIL**

- [ ] **Step 3: Implementar happy path completo**

Substitui o stub `Start` por:

```go
func (b *BootstrapService) Start(ctx context.Context, taskID string) (*StartedBootstrap, error) {
	if err := sandbox.SandboxAvailable(); err != nil {
		return nil, fmt.Errorf("%w: %v", ErrSandboxUnavailable, err)
	}
	terminal, err := sandbox.DetectTerminal()
	if err != nil {
		return nil, fmt.Errorf("%w: %v", ErrSandboxUnavailable, err)
	}

	task, err := b.tasks.Get(ctx, taskID)
	if err != nil {
		return nil, err
	}
	if IsTerminal(task.State) {
		return nil, ErrTaskInTerminalState
	}

	b.mu.Lock()
	if existing, ok := b.active[taskID]; ok {
		b.mu.Unlock()
		return b.viewFromEntry(existing), nil
	}
	b.mu.Unlock()

	cwd, err := b.resolveCwd(ctx, taskID, task)
	if err != nil {
		return nil, err
	}

	manifestPath := filepath.Join(cwd, ".orchestrator", "run.yml")
	if _, err := os.Stat(manifestPath); err == nil {
		return nil, ErrManifestAlreadyExists
	}

	profile, err := b.catalog.ResolveProfile(b.catalog.FallbackPermissionProfile)
	if err != nil {
		return nil, fmt.Errorf("resolve fallback profile: %w", err)
	}

	orchDir := filepath.Join(cwd, ".orchestrator")
	if err := os.MkdirAll(orchDir, 0o755); err != nil {
		return nil, fmt.Errorf("mkdir .orchestrator: %w", err)
	}
	promptPath := filepath.Join(orchDir, "BOOTSTRAP_PROMPT.md")
	if err := os.WriteFile(promptPath, []byte(bootstrapPromptTemplate), 0o644); err != nil {
		return nil, fmt.Errorf("write BOOTSTRAP_PROMPT.md: %w", err)
	}
	if err := sandbox.WriteAijailConfig(cwd, profile.ClaudeArgs, nil); err != nil {
		_ = os.Remove(promptPath)
		return nil, fmt.Errorf("write .ai-jail: %w", err)
	}

	watcher, err := fsnotify.NewWatcher()
	if err != nil {
		_ = sandbox.RemoveAijailConfig(cwd)
		_ = os.Remove(promptPath)
		return nil, fmt.Errorf("new fsnotify watcher: %w", err)
	}
	if err := watcher.Add(orchDir); err != nil {
		_ = watcher.Close()
		_ = sandbox.RemoveAijailConfig(cwd)
		_ = os.Remove(promptPath)
		return nil, fmt.Errorf("watcher.Add: %w", err)
	}

	handle, err := b.runtime.Spawn(ctx, sandbox.RuntimeSpec{Cwd: cwd, Terminal: terminal})
	if err != nil {
		_ = watcher.Close()
		_ = sandbox.RemoveAijailConfig(cwd)
		_ = os.Remove(promptPath)
		return nil, fmt.Errorf("spawn bootstrap claude: %w", err)
	}

	watcherCtx, cancel := context.WithCancel(context.Background())
	ready := make(chan struct{})
	entry := &bootstrapEntry{
		sessionID:    uuid.NewString(),
		cwd:          cwd,
		handle:       handle,
		watcher:      watcher,
		cancel:       cancel,
		watcherReady: ready,
		startedAt:    time.Now(),
	}

	b.mu.Lock()
	// Double-check under lock — caller raced us.
	if existing, ok := b.active[taskID]; ok {
		b.mu.Unlock()
		_ = b.runtime.Kill(ctx, handle)
		cancel()
		_ = watcher.Close()
		_ = sandbox.RemoveAijailConfig(cwd)
		_ = os.Remove(promptPath)
		return b.viewFromEntry(existing), nil
	}
	b.active[taskID] = entry
	b.mu.Unlock()

	// Stage 5 wires the watcher goroutine. For now, close ready immediately
	// so happy-path tests don't block on it.
	close(ready)
	_ = watcherCtx // used by Stage 5

	return b.viewFromEntry(entry), nil
}

func (b *BootstrapService) viewFromEntry(e *bootstrapEntry) *StartedBootstrap {
	return &StartedBootstrap{
		SessionID:    e.sessionID,
		Cwd:          e.cwd,
		ManifestPath: filepath.Join(e.cwd, ".orchestrator", "run.yml"),
		PromptPath:   filepath.Join(e.cwd, ".orchestrator", "BOOTSTRAP_PROMPT.md"),
		WatcherReady: e.watcherReady,
	}
}
```

Imports adicionais: `github.com/google/uuid`.

- [ ] **Step 4: Run, PASS**

```bash
go test -tags webkit2_41 -run TestStart_HappyPath ./internal/core/...
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add internal/core/bootstrap.go internal/core/bootstrap_test.go
git commit -m "$(cat <<'EOF'
feat(F10.6.B.3.5): Start happy path (write prompt + spawn + watcher init)

Writes .orchestrator/BOOTSTRAP_PROMPT.md (from embedded template) and
.ai-jail (claude args from fallback profile). Initializes
fsnotify.Watcher on .orchestrator/ dir (Add must happen BEFORE the
spawn so the watcher is ready by the time Claude writes anything).
Spawns the ephemeral session via the same sandbox.Runtime seam used by
SessionsService. Inserts an entry in the per-task map keyed by task_id.

Watcher goroutine is wired in Stage 5; for now WatcherReady is closed
immediately so happy-path tests don't deadlock.

Double-check-under-lock guards against two concurrent Start calls
seeing an empty map (both would otherwise spawn).
EOF
)"
```

### Task 3.6: TestStart_IdempotentReturnsExisting

- [ ] **Step 1: Write failing test**

```go
func TestStart_IdempotentReturnsExisting(t *testing.T) {
	env := newBootstrapTestEnv(t)
	defer env.cleanup()
	ctx := context.Background()

	first, err := env.svc.Start(ctx, env.taskID)
	if err != nil {
		t.Fatalf("Start #1: %v", err)
	}
	second, err := env.svc.Start(ctx, env.taskID)
	if err != nil {
		t.Fatalf("Start #2: %v", err)
	}
	if first.SessionID != second.SessionID {
		t.Errorf("SessionID changed: %q vs %q", first.SessionID, second.SessionID)
	}
	if env.runtime.SpawnCount() != 1 {
		t.Errorf("spawn count=%d, want 1", env.runtime.SpawnCount())
	}
}
```

- [ ] **Step 2: Run, expect PASS** (early-return path em Start já cobre)

```bash
go test -tags webkit2_41 -run TestStart_IdempotentReturnsExisting ./internal/core/...
```

Expected: PASS.

- [ ] **Step 3: Commit**

```bash
git add internal/core/bootstrap_test.go
git commit -m "test(F10.6.B.3.6): TestStart_IdempotentReturnsExisting"
```

### Task 3.7: TestStart_SpawnFailsCleansUp

- [ ] **Step 1: Write failing test**

```go
func TestStart_SpawnFailsCleansUp(t *testing.T) {
	env := newBootstrapTestEnv(t)
	defer env.cleanup()
	env.runtime.spawnErr = errors.New("forced spawn failure")

	_, err := env.svc.Start(context.Background(), env.taskID)
	if err == nil || !strings.Contains(err.Error(), "spawn bootstrap claude") {
		t.Fatalf("Start: err=%v, want spawn failure", err)
	}
	// Prompt and .ai-jail should NOT remain on disk.
	if _, err := os.Stat(filepath.Join(env.worktree, ".orchestrator", "BOOTSTRAP_PROMPT.md")); err == nil {
		t.Error("BOOTSTRAP_PROMPT.md leaked on disk after spawn failure")
	}
	if _, err := os.Stat(filepath.Join(env.worktree, ".ai-jail")); err == nil {
		t.Error(".ai-jail leaked on disk after spawn failure")
	}
}
```

- [ ] **Step 2: Run, expect PASS** (já implementado no Step 3 da Task 3.5)

```bash
go test -tags webkit2_41 -run TestStart_SpawnFailsCleansUp ./internal/core/...
```

Expected: PASS.

- [ ] **Step 3: Commit**

```bash
git add internal/core/bootstrap_test.go
git commit -m "test(F10.6.B.3.7): TestStart_SpawnFailsCleansUp"
```

### Task 3.8: TestStart_WorktreeCreationFails

- [ ] **Step 1: Write failing test (precisa de fakeWtSvc com erro injetável)**

Atualiza `newBootstrapTestEnv` pra aceitar opção que injete erro em `wtSvc.CreateForTask` — ou (mais simples) deleta a linha de worktree e instala um `fakeGit` que retorna erro.

```go
func TestStart_WorktreeCreationFails(t *testing.T) {
	env := newBootstrapTestEnv(t)
	defer env.cleanup()
	ctx := context.Background()

	// Drop the seeded worktree so CreateForTask runs; configure fakeGit to fail.
	wts, _ := env.svc.worktrees.ListByTask(ctx, env.taskID)
	for _, wt := range wts {
		_ = env.svc.worktrees.Delete(ctx, wt.ID)
	}
	// Replace wtSvc with one whose git fails.
	failingGit := &fakeGit{addWorktreeErr: errors.New("forced git failure")}
	reposRepo := store.NewRepositoriesRepo(env.svc.worktrees.DB())  // adjust if needed
	env.svc.wtSvc = NewWorktreesService(env.svc.worktrees, reposRepo, env.svc.projectsRepo /* if exposed */, failingGit, env.emitter)

	_, err := env.svc.Start(ctx, env.taskID)
	if err == nil || !strings.Contains(err.Error(), "create worktrees for task") {
		t.Fatalf("Start: err=%v, want worktree creation failure", err)
	}
	if env.runtime.SpawnCount() != 0 {
		t.Errorf("spawn count=%d, want 0", env.runtime.SpawnCount())
	}
}
```

> **Note:** the swap-out-wtSvc above is awkward. **Cleaner alternative:** at the top of `newBootstrapTestEnv`, accept an optional `t.Cleanup`-managed config that lets the test pre-inject a `fakeGit` with `addWorktreeErr`. Refactor as needed — keep the test's assertion intent (no spawn, error message contains "create worktrees for task").

- [ ] **Step 2: Run, expect PASS** (Start already propagates resolveCwd errors)

- [ ] **Step 3: Commit**

```bash
git add internal/core/bootstrap_test.go
git commit -m "test(F10.6.B.3.8): TestStart_WorktreeCreationFails"
```

### Task 3.9: TestStart_MultiRepoWorktreeUsesParentDir

- [ ] **Step 1: Write failing test**

Cria um segundo worktree pro mesmo task; espera `cwd` = `filepath.Dir(wts[0].Path)`.

```go
func TestStart_MultiRepoWorktreeUsesParentDir(t *testing.T) {
	env := newBootstrapTestEnv(t)
	defer env.cleanup()
	ctx := context.Background()

	// Add a second repo + worktree in the same parent dir.
	parent := filepath.Dir(env.worktree)
	wt2 := filepath.Join(parent, "repo-b")
	if err := os.MkdirAll(wt2, 0o755); err != nil { t.Fatalf("mkdir: %v", err) }

	// Insert repo-b row + worktree linked to env.taskID.
	// (Adjust depending on what newBootstrapTestEnv exposes — may need to keep
	// projectsRepo + reposRepo on env for tests like this.)
	// ... (snippet of repo+wt insert)

	started, err := env.svc.Start(ctx, env.taskID)
	if err != nil {
		t.Fatalf("Start: %v", err)
	}
	if started.Cwd != parent {
		t.Errorf("Cwd=%q, want %q (parent of wts[0])", started.Cwd, parent)
	}
}
```

- [ ] **Step 2: Run, PASS** (already implemented by resolveCwd's `filepath.Dir(wts[0].Path)` branch when `len(wts) > 1`)

- [ ] **Step 3: Commit**

```bash
git add internal/core/bootstrap_test.go
git commit -m "test(F10.6.B.3.9): multi-repo task uses parent dir as cwd"
```

---

## Stage 4 — `Cancel` + `CleanupForTask`

Objetivo: cleanup explícito que mata runtime, fecha watcher, remove arquivos, remove entrada do map. Idempotente.

### Task 4.1: TestCancel_HappyPath

- [ ] **Step 1: Write failing test**

```go
func TestCancel_HappyPath(t *testing.T) {
	env := newBootstrapTestEnv(t)
	defer env.cleanup()
	ctx := context.Background()

	started, err := env.svc.Start(ctx, env.taskID)
	if err != nil { t.Fatalf("Start: %v", err) }
	<-started.WatcherReady

	if err := env.svc.Cancel(ctx, env.taskID); err != nil {
		t.Fatalf("Cancel: %v", err)
	}

	if env.runtime.KillCount() != 1 {
		t.Errorf("kill count=%d, want 1", env.runtime.KillCount())
	}
	if _, err := os.Stat(started.PromptPath); err == nil {
		t.Error("BOOTSTRAP_PROMPT.md leaked after Cancel")
	}
	// Entry removed
	env.svc.mu.Lock()
	_, ok := env.svc.active[env.taskID]
	env.svc.mu.Unlock()
	if ok {
		t.Error("entry still in active map after Cancel")
	}
}
```

- [ ] **Step 2: Run, FAIL** (Cancel undefined)

- [ ] **Step 3: Implementar Cancel**

```go
func (b *BootstrapService) Cancel(ctx context.Context, taskID string) error {
	b.mu.Lock()
	entry, ok := b.active[taskID]
	if !ok {
		b.mu.Unlock()
		return nil
	}
	delete(b.active, taskID)
	b.mu.Unlock()

	entry.cancel()
	_ = entry.watcher.Close()
	if err := b.runtime.Kill(ctx, entry.handle); err != nil {
		log.Printf("bootstrap cancel: kill pid %d: %v", entry.handle.PID, err)
	}
	_ = os.Remove(filepath.Join(entry.cwd, ".orchestrator", "BOOTSTRAP_PROMPT.md"))
	return nil
}
```

Adiciona `"log"` ao import.

- [ ] **Step 4: Run, PASS**

```bash
go test -tags webkit2_41 -run TestCancel_HappyPath ./internal/core/...
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add internal/core/bootstrap.go internal/core/bootstrap_test.go
git commit -m "$(cat <<'EOF'
feat(F10.6.B.4.1): Cancel kills runtime + closes watcher + removes prompt

Tolerant: kill failures are logged, not propagated. Locks the map for
the delete, then does the side-effects unlocked (Kill can take real
time and we don't want to block other Start/Cancel calls).
EOF
)"
```

### Task 4.2: TestCancel_UnknownAndIdempotent

- [ ] **Step 1: Write tests**

```go
func TestCancel_UnknownTask(t *testing.T) {
	env := newBootstrapTestEnv(t)
	defer env.cleanup()
	if err := env.svc.Cancel(context.Background(), "no-such-task"); err != nil {
		t.Errorf("Cancel on unknown: err=%v, want nil", err)
	}
}

func TestCancel_Idempotent(t *testing.T) {
	env := newBootstrapTestEnv(t)
	defer env.cleanup()
	ctx := context.Background()
	if _, err := env.svc.Start(ctx, env.taskID); err != nil { t.Fatalf("Start: %v", err) }
	if err := env.svc.Cancel(ctx, env.taskID); err != nil { t.Fatalf("Cancel #1: %v", err) }
	if err := env.svc.Cancel(ctx, env.taskID); err != nil { t.Errorf("Cancel #2: err=%v, want nil (no-op)", err) }
	if env.runtime.KillCount() != 1 {
		t.Errorf("kill count=%d, want 1 (second Cancel should not kill again)", env.runtime.KillCount())
	}
}
```

- [ ] **Step 2: Run, PASS** (implementation already returns nil when entry absent)

- [ ] **Step 3: Commit**

```bash
git add internal/core/bootstrap_test.go
git commit -m "test(F10.6.B.4.2): Cancel idempotent + no-op on unknown task"
```

### Task 4.3: CleanupForTask delegates

- [ ] **Step 1: Write failing test**

```go
func TestCleanupForTask_DelegatesCancel(t *testing.T) {
	env := newBootstrapTestEnv(t)
	defer env.cleanup()
	ctx := context.Background()
	if _, err := env.svc.Start(ctx, env.taskID); err != nil { t.Fatalf("Start: %v", err) }

	if err := env.svc.CleanupForTask(ctx, env.taskID); err != nil { t.Fatalf("CleanupForTask: %v", err) }
	if env.runtime.KillCount() != 1 {
		t.Errorf("kill count=%d, want 1", env.runtime.KillCount())
	}
}
```

- [ ] **Step 2: Run, FAIL**

- [ ] **Step 3: Implementar**

```go
func (b *BootstrapService) CleanupForTask(ctx context.Context, taskID string) error {
	return b.Cancel(ctx, taskID)
}
```

- [ ] **Step 4: Run, PASS**

- [ ] **Step 5: Commit**

```bash
git add internal/core/bootstrap.go internal/core/bootstrap_test.go
git commit -m "feat(F10.6.B.4.3): CleanupForTask delegates to Cancel"
```

---

## Stage 5 — Watcher goroutine + `handleDetection`

Objetivo: o coração race-safe. Goroutine consome `fsnotify.Watcher.Events`; `handleDetection` valida o YAML e — se válido — reivindica ownership pra fazer cleanup uma única vez (mesmo sob Cancel concorrente). Se inválido, mantém tudo vivo.

### Task 5.1: Wire the watcher goroutine + WatcherReady barrier

**Files:**
- Modify: `internal/core/bootstrap.go`

- [ ] **Step 1: Adicionar a goroutine no fim de `Start`**

Substitui o trecho:

```go
	// Stage 5 wires the watcher goroutine. For now, close ready immediately
	// so happy-path tests don't block on it.
	close(ready)
	_ = watcherCtx // used by Stage 5
```

Por:

```go
	go b.watchManifest(watcherCtx, taskID, entry, manifestPath, ready)
```

E adiciona a função:

```go
func (b *BootstrapService) watchManifest(
	ctx context.Context,
	taskID string,
	entry *bootstrapEntry,
	manifestPath string,
	ready chan struct{},
) {
	defer entry.watcher.Close()
	close(ready) // signals tests + production callers that fsnotify is now consuming events

	for {
		select {
		case <-ctx.Done():
			return
		case ev, ok := <-entry.watcher.Events:
			if !ok {
				return
			}
			if filepath.Base(ev.Name) != "run.yml" {
				continue
			}
			if ev.Op&(fsnotify.Create|fsnotify.Write) == 0 {
				continue
			}
			b.handleDetection(taskID, entry, manifestPath)
		case err, ok := <-entry.watcher.Errors:
			if !ok {
				return
			}
			log.Printf("bootstrap watcher (task %s): error: %v", taskID, err)
		}
	}
}
```

- [ ] **Step 2: Adicionar `handleDetection` (skeleton — emite com `valid=true` temporariamente, sem validação real)**

```go
func (b *BootstrapService) handleDetection(taskID string, entry *bootstrapEntry, manifestPath string) {
	content, err := os.ReadFile(manifestPath)
	if err != nil {
		log.Printf("bootstrap detection (task %s): read manifest: %v", taskID, err)
		return
	}

	valid, errs := b.validateManifestBytes(content)

	var claimed *bootstrapEntry
	b.mu.Lock()
	if _, ok := b.active[taskID]; !ok {
		b.mu.Unlock()
		return // Cancel took ownership; do nothing
	}
	if valid {
		delete(b.active, taskID)
		claimed = entry
	}
	b.mu.Unlock()

	if claimed != nil {
		claimed.cancel()
		if err := b.runtime.Kill(context.Background(), claimed.handle); err != nil {
			log.Printf("bootstrap detection (task %s): kill pid %d: %v", taskID, claimed.handle.PID, err)
		}
		_ = os.Remove(filepath.Join(claimed.cwd, ".orchestrator", "BOOTSTRAP_PROMPT.md"))
	}

	if errs == nil {
		errs = []string{}
	}
	b.bus.Emit("bootstrap.proposed", BootstrapProposedPayload{
		TaskID:       taskID,
		SessionID:    entry.sessionID,
		ManifestText: string(content),
		Valid:        valid,
		Errors:       errs,
	})
}

// validateManifestBytes runs LoadManifest semantics directly from bytes, since
// the watcher reads the file (LoadManifest opens by path — fine, but we have
// the bytes already and want to keep the error list).
func (b *BootstrapService) validateManifestBytes(data []byte) (valid bool, errs []string) {
	var m ManifestSpec
	if err := yaml.Unmarshal(data, &m); err != nil {
		return false, []string{fmt.Sprintf("yaml: %v", err)}
	}
	if err := m.Validate(); err != nil {
		// ManifestSpec.Validate joins problems with "; "; split for the UI.
		msg := strings.TrimPrefix(err.Error(), "runs: manifest invalid: ")
		return false, strings.Split(msg, "; ")
	}
	return true, nil
}
```

Imports adicionais: `"gopkg.in/yaml.v3"`, `"strings"`.

- [ ] **Step 3: Compila smoke**

```bash
go vet -tags webkit2_41 ./internal/core/...
```

Expected: sem output.

- [ ] **Step 4: Roda toda a suíte; tests anteriores devem continuar passando**

```bash
go test -tags webkit2_41 ./internal/core/...
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add internal/core/bootstrap.go
git commit -m "$(cat <<'EOF'
feat(F10.6.B.5.1): wire watcher goroutine + handleDetection skeleton

watchManifest defers watcher.Close(), closes the ready channel BEFORE
the first select (so callers can sync), filters by basename + op.
handleDetection reads + validates + claims ownership atomically under
the service mutex; if valid, performs unlocked side-effects (runtime
Kill, prompt removal). Always emits bootstrap.proposed, valid or not.
EOF
)"
```

### Task 5.2: TestDetection_ValidEmitsAndCleansUp

- [ ] **Step 1: Write failing test (validates the END-TO-END detection path)**

```go
func TestDetection_ValidEmitsAndCleansUp(t *testing.T) {
	env := newBootstrapTestEnv(t)
	defer env.cleanup()
	ctx := context.Background()

	started, err := env.svc.Start(ctx, env.taskID)
	if err != nil { t.Fatalf("Start: %v", err) }
	<-started.WatcherReady

	manifest := []byte(`version: "1"
services:
  web:
    image: nginx:latest
    port: 80
`)
	if err := os.WriteFile(started.ManifestPath, manifest, 0o644); err != nil {
		t.Fatalf("write manifest: %v", err)
	}

	if !waitForEvent(env.emitter, "bootstrap.proposed", 2*time.Second) {
		t.Fatal("bootstrap.proposed not emitted within 2s")
	}

	// Inspect payload
	calls := env.emitter.Snapshot()
	last := calls[len(calls)-1]
	p, ok := last.Payload.(BootstrapProposedPayload)
	if !ok {
		t.Fatalf("payload type=%T, want BootstrapProposedPayload", last.Payload)
	}
	if !p.Valid {
		t.Errorf("Valid=false, errors=%v", p.Errors)
	}
	if p.ManifestText != string(manifest) {
		t.Errorf("ManifestText mismatch")
	}
	if p.TaskID != env.taskID {
		t.Errorf("TaskID=%q, want %q", p.TaskID, env.taskID)
	}

	// Cleanup happened
	if _, err := os.Stat(started.PromptPath); err == nil {
		t.Error("BOOTSTRAP_PROMPT.md still on disk after valid detection")
	}
	env.svc.mu.Lock()
	_, stillActive := env.svc.active[env.taskID]
	env.svc.mu.Unlock()
	if stillActive {
		t.Error("entry still in active map after valid detection")
	}
	if env.runtime.KillCount() != 1 {
		t.Errorf("kill count=%d, want 1", env.runtime.KillCount())
	}
}
```

> **Need:** `waitForEvent` and `FakeEmitter.Snapshot` helpers. If they don't exist, add them.

- [ ] **Step 2: Adicionar helpers se faltarem**

Em `internal/events/bus.go`:

```go
func (f *FakeEmitter) Snapshot() []EmitCall {
	f.mu.Lock()
	defer f.mu.Unlock()
	out := make([]EmitCall, len(f.Calls))
	copy(out, f.Calls)
	return out
}
```

Em `internal/core/bootstrap_test.go`:

```go
func waitForEvent(emitter *events.FakeEmitter, name string, timeout time.Duration) bool {
	deadline := time.Now().Add(timeout)
	for time.Now().Before(deadline) {
		for _, c := range emitter.Snapshot() {
			if c.Name == name {
				return true
			}
		}
		time.Sleep(10 * time.Millisecond)
	}
	return false
}
```

- [ ] **Step 3: Run, PASS**

```bash
go test -tags webkit2_41 -run TestDetection_ValidEmitsAndCleansUp ./internal/core/... -race
```

Expected: PASS (also clean under `-race`).

- [ ] **Step 4: Commit**

```bash
git add internal/core/bootstrap.go internal/core/bootstrap_test.go internal/events/bus.go
git commit -m "$(cat <<'EOF'
test(F10.6.B.5.2): TestDetection_ValidEmitsAndCleansUp

End-to-end through real fsnotify: write a valid manifest, assert that
bootstrap.proposed fires with valid=true, prompt is removed, entry
leaves the active map, runtime.Kill was called exactly once.

Adds FakeEmitter.Snapshot() (mu-safe copy of Calls slice) and a
waitForEvent helper that polls the snapshot until deadline.
EOF
)"
```

### Task 5.3: TestDetection_InvalidKeepsWatching

- [ ] **Step 1: Write failing test**

```go
func TestDetection_InvalidKeepsWatching(t *testing.T) {
	env := newBootstrapTestEnv(t)
	defer env.cleanup()
	ctx := context.Background()

	started, err := env.svc.Start(ctx, env.taskID)
	if err != nil { t.Fatalf("Start: %v", err) }
	<-started.WatcherReady

	// First write: invalid (missing version)
	bad := []byte("services:\n  web:\n    image: nginx\n")
	if err := os.WriteFile(started.ManifestPath, bad, 0o644); err != nil {
		t.Fatalf("write bad: %v", err)
	}
	if !waitForEvent(env.emitter, "bootstrap.proposed", 2*time.Second) {
		t.Fatal("first bootstrap.proposed not emitted")
	}
	calls := env.emitter.Snapshot()
	if p := calls[len(calls)-1].Payload.(BootstrapProposedPayload); p.Valid {
		t.Errorf("first emit valid=true, want false (bad yaml)")
	}

	// Entry, prompt, watcher all still live
	if _, err := os.Stat(started.PromptPath); err != nil {
		t.Error("BOOTSTRAP_PROMPT.md removed on invalid (should persist)")
	}
	env.svc.mu.Lock()
	_, stillActive := env.svc.active[env.taskID]
	env.svc.mu.Unlock()
	if !stillActive {
		t.Error("entry removed from active map on invalid (should persist)")
	}

	// Now drop a valid manifest
	good := []byte(`version: "1"
services:
  web:
    image: nginx
`)
	if err := os.WriteFile(started.ManifestPath, good, 0o644); err != nil {
		t.Fatalf("write good: %v", err)
	}

	// Wait for SECOND emit (valid)
	deadline := time.Now().Add(2 * time.Second)
	for time.Now().Before(deadline) {
		snap := env.emitter.Snapshot()
		if len(snap) >= 2 {
			last := snap[len(snap)-1].Payload.(BootstrapProposedPayload)
			if last.Valid {
				return // success
			}
		}
		time.Sleep(10 * time.Millisecond)
	}
	t.Fatal("second (valid) bootstrap.proposed never emitted")
}
```

- [ ] **Step 2: Run, PASS** (handleDetection already keeps state on invalid)

```bash
go test -tags webkit2_41 -run TestDetection_InvalidKeepsWatching ./internal/core/... -race
```

Expected: PASS.

- [ ] **Step 3: Commit**

```bash
git add internal/core/bootstrap_test.go
git commit -m "$(cat <<'EOF'
test(F10.6.B.5.3): invalid manifest keeps watcher alive

First drop is invalid YAML — emit bootstrap.proposed{valid:false} but
entry, prompt, watcher remain. Second drop (valid) triggers cleanup +
emit valid:true. Models the realistic flow where the user has Claude
fix the manifest iteratively.
EOF
)"
```

### Task 5.4: TestDetection_DuplicateEventsIdempotent

- [ ] **Step 1: Write failing test**

```go
func TestDetection_DuplicateEventsIdempotent(t *testing.T) {
	env := newBootstrapTestEnv(t)
	defer env.cleanup()
	ctx := context.Background()

	started, err := env.svc.Start(ctx, env.taskID)
	if err != nil { t.Fatalf("Start: %v", err) }
	<-started.WatcherReady

	manifest := []byte(`version: "1"
services:
  web:
    image: nginx
`)
	// Write 3 times rapidly. fsnotify may fire 3+ events.
	for i := 0; i < 3; i++ {
		if err := os.WriteFile(started.ManifestPath, manifest, 0o644); err != nil {
			t.Fatalf("write #%d: %v", i, err)
		}
		time.Sleep(5 * time.Millisecond)
	}

	// Wait briefly to let events drain.
	time.Sleep(200 * time.Millisecond)

	// Exactly 1 emit, 1 Kill.
	emitsValid := 0
	for _, c := range env.emitter.Snapshot() {
		if c.Name == "bootstrap.proposed" {
			if c.Payload.(BootstrapProposedPayload).Valid {
				emitsValid++
			}
		}
	}
	if emitsValid != 1 {
		t.Errorf("emitsValid=%d, want exactly 1", emitsValid)
	}
	if env.runtime.KillCount() != 1 {
		t.Errorf("KillCount=%d, want 1", env.runtime.KillCount())
	}
}
```

- [ ] **Step 2: Run, PASS** (claim-under-lock makes subsequent events see `!ok`)

```bash
go test -tags webkit2_41 -run TestDetection_DuplicateEventsIdempotent ./internal/core/... -race
```

Expected: PASS.

- [ ] **Step 3: Commit**

```bash
git add internal/core/bootstrap_test.go
git commit -m "test(F10.6.B.5.4): duplicate fsnotify events emit cleanup once"
```

### Task 5.5: TestDetection_RaceWithCancel

- [ ] **Step 1: Write failing test**

```go
func TestDetection_RaceWithCancel(t *testing.T) {
	for i := 0; i < 20; i++ {
		t.Run(fmt.Sprintf("iter-%d", i), func(t *testing.T) {
			env := newBootstrapTestEnv(t)
			defer env.cleanup()
			ctx := context.Background()

			started, err := env.svc.Start(ctx, env.taskID)
			if err != nil { t.Fatalf("Start: %v", err) }
			<-started.WatcherReady

			manifest := []byte(`version: "1"
services:
  web:
    image: nginx
`)

			var wg sync.WaitGroup
			wg.Add(2)
			go func() { defer wg.Done(); _ = os.WriteFile(started.ManifestPath, manifest, 0o644) }()
			go func() { defer wg.Done(); _ = env.svc.Cancel(ctx, env.taskID) }()
			wg.Wait()

			// Give the watcher a chance to consume.
			time.Sleep(100 * time.Millisecond)

			// Exactly one Kill regardless of who won.
			if k := env.runtime.KillCount(); k != 1 {
				t.Errorf("KillCount=%d, want 1 (race winner cleans up exactly once)", k)
			}
			// Entry must be gone.
			env.svc.mu.Lock()
			_, stillActive := env.svc.active[env.taskID]
			env.svc.mu.Unlock()
			if stillActive {
				t.Error("entry still in active map after race")
			}
		})
	}
}
```

- [ ] **Step 2: Run with race detector**

```bash
go test -tags webkit2_41 -run TestDetection_RaceWithCancel ./internal/core/... -race -count=1
```

Expected: PASS, no race warnings.

- [ ] **Step 3: Commit**

```bash
git add internal/core/bootstrap_test.go
git commit -m "$(cat <<'EOF'
test(F10.6.B.5.5): race between detection and Cancel — exactly 1 cleanup

20 iterations. In each, file write and Cancel fire concurrently. The
single-locked-claim section guarantees one cleanup. Runs under -race
to catch any latent ordering bug.
EOF
)"
```

---

## Stage 6 — `BootstrapAPI` Wails layer

Objetivo: tipo JSON-serializável `BootstrapView` (sem `WatcherReady`), métodos `Start` e `Cancel` que ponteam pra `BootstrapService`, mapeamento de erros pra mensagens human-readable.

### Task 6.1: BootstrapAPI skeleton + Start

**Files:**
- Create: `internal/api/bootstrap.go`
- Create: `internal/api/bootstrap_test.go`

- [ ] **Step 1: Write skeleton**

`internal/api/bootstrap.go`:

```go
package api

import (
	"context"
	"errors"
	"fmt"

	"github.com/marcosdid/jarvis/internal/core"
)

// BootstrapService is the API-layer view of *core.BootstrapService.
type BootstrapService interface {
	Start(ctx context.Context, taskID string) (*core.StartedBootstrap, error)
	Cancel(ctx context.Context, taskID string) error
	CleanupForTask(ctx context.Context, taskID string) error
}

type BootstrapAPI struct {
	svc BootstrapService
}

func NewBootstrapAPI(svc BootstrapService) *BootstrapAPI {
	return &BootstrapAPI{svc: svc}
}

// BootstrapView is the JSON-serializable projection of core.StartedBootstrap
// (the WatcherReady channel is stripped — it's a Go-only sync barrier for tests).
type BootstrapView struct {
	SessionID    string `json:"session_id"`
	Cwd          string `json:"cwd"`
	ManifestPath string `json:"manifest_path"`
	PromptPath   string `json:"prompt_path"`
}

func (a *BootstrapAPI) Start(taskID string) (BootstrapView, error) {
	started, err := a.svc.Start(context.Background(), taskID)
	if err != nil {
		return BootstrapView{}, translateBootstrapErr(err)
	}
	return BootstrapView{
		SessionID:    started.SessionID,
		Cwd:          started.Cwd,
		ManifestPath: started.ManifestPath,
		PromptPath:   started.PromptPath,
	}, nil
}

func (a *BootstrapAPI) Cancel(taskID string) error {
	return a.svc.Cancel(context.Background(), taskID)
}

func translateBootstrapErr(err error) error {
	switch {
	case errors.Is(err, core.ErrManifestAlreadyExists):
		return fmt.Errorf("manifest already exists; click ▶ Run again to start the stack")
	case errors.Is(err, core.ErrTaskInTerminalState):
		return fmt.Errorf("task is already in a terminal state; bootstrap not allowed")
	case errors.Is(err, core.ErrSandboxUnavailable):
		return fmt.Errorf("sandbox unavailable (ai-jail or terminal missing): %v", err)
	default:
		return err
	}
}
```

- [ ] **Step 2: Write test scaffold**

`internal/api/bootstrap_test.go`:

```go
package api

import (
	"context"
	"errors"
	"strings"
	"testing"

	"github.com/marcosdid/jarvis/internal/core"
)

type fakeBootstrapSvc struct {
	startResult *core.StartedBootstrap
	startErr    error
	cancelErr   error
	startCalls  int
	cancelCalls int
}

func (f *fakeBootstrapSvc) Start(_ context.Context, _ string) (*core.StartedBootstrap, error) {
	f.startCalls++
	return f.startResult, f.startErr
}
func (f *fakeBootstrapSvc) Cancel(_ context.Context, _ string) error {
	f.cancelCalls++
	return f.cancelErr
}
func (f *fakeBootstrapSvc) CleanupForTask(_ context.Context, _ string) error { return nil }

func TestBootstrapAPI_Start_ReturnsView(t *testing.T) {
	fake := &fakeBootstrapSvc{
		startResult: &core.StartedBootstrap{
			SessionID:    "s-1",
			Cwd:          "/tmp/wt",
			ManifestPath: "/tmp/wt/.orchestrator/run.yml",
			PromptPath:   "/tmp/wt/.orchestrator/BOOTSTRAP_PROMPT.md",
		},
	}
	a := NewBootstrapAPI(fake)
	view, err := a.Start("t1")
	if err != nil { t.Fatalf("Start: %v", err) }
	if view.SessionID != "s-1" { t.Errorf("SessionID=%q", view.SessionID) }
	if view.Cwd != "/tmp/wt" { t.Errorf("Cwd=%q", view.Cwd) }
	if view.ManifestPath != "/tmp/wt/.orchestrator/run.yml" { t.Errorf("ManifestPath=%q", view.ManifestPath) }
	if view.PromptPath != "/tmp/wt/.orchestrator/BOOTSTRAP_PROMPT.md" { t.Errorf("PromptPath=%q", view.PromptPath) }
}

func TestBootstrapAPI_Start_ManifestAlreadyExists(t *testing.T) {
	fake := &fakeBootstrapSvc{startErr: core.ErrManifestAlreadyExists}
	a := NewBootstrapAPI(fake)
	_, err := a.Start("t1")
	if err == nil || !strings.Contains(err.Error(), "click ▶ Run again") {
		t.Errorf("err=%v, want human-readable message", err)
	}
}

func TestBootstrapAPI_Start_TaskTerminal(t *testing.T) {
	fake := &fakeBootstrapSvc{startErr: core.ErrTaskInTerminalState}
	a := NewBootstrapAPI(fake)
	_, err := a.Start("t1")
	if err == nil || !strings.Contains(err.Error(), "terminal state") {
		t.Errorf("err=%v, want terminal-state message", err)
	}
}

func TestBootstrapAPI_Cancel_NoOpOnUnknown(t *testing.T) {
	fake := &fakeBootstrapSvc{} // cancelErr nil
	a := NewBootstrapAPI(fake)
	if err := a.Cancel("t-unknown"); err != nil {
		t.Errorf("Cancel: err=%v, want nil", err)
	}
}

func TestBootstrapAPI_Cancel_PropagatesUnexpectedErr(t *testing.T) {
	fake := &fakeBootstrapSvc{cancelErr: errors.New("disk full")}
	a := NewBootstrapAPI(fake)
	if err := a.Cancel("t1"); err == nil {
		t.Error("Cancel: got nil, want error")
	}
}
```

- [ ] **Step 3: Run**

```bash
go test -tags webkit2_41 ./internal/api/...
```

Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add internal/api/bootstrap.go internal/api/bootstrap_test.go
git commit -m "$(cat <<'EOF'
feat(F10.6.B.6.1): BootstrapAPI Wails layer + error translation

JSON-serializable BootstrapView strips StartedBootstrap.WatcherReady
(channels can't cross the Wails boundary). translateBootstrapErr maps
sentinel core errors to human messages the UI surfaces directly.
EOF
)"
```

---

## Stage 7 — RunsAPI `StartRunResult` refactor

Objetivo: trocar `RunsAPI.Start` de `(RunView, error)` para `(StartRunResult{Run?, Bootstrap?}, error)`. Quando `ErrManifestMissing` ocorre, retorna `StartRunResult{Bootstrap: &BootstrapHint{Reason:"manifest_missing"}}` em vez de erro. UI usa shape pra decidir.

### Task 7.1: StartRunResult types + Start refactor

**Files:**
- Modify: `internal/api/runs.go`

- [ ] **Step 1: Adicionar types e refatorar Start**

Substitui `Start` em `internal/api/runs.go`:

```go
// StartRunResult is the union "run succeeded OR needs bootstrap". Exactly one
// of Run / Bootstrap is non-nil on success. Errors still propagate normally
// (sandbox-unavailable, docker-down, etc.) — only the manifest-missing case
// turns into a hint instead of an error.
type StartRunResult struct {
	Run       *RunView       `json:"run,omitempty"`
	Bootstrap *BootstrapHint `json:"bootstrap,omitempty"`
}

type BootstrapHint struct {
	Reason string `json:"reason"` // "manifest_missing" today; extensible
}

func (a *RunsAPI) Start(taskID string) (StartRunResult, error) {
	run, err := a.svc.StartRun(context.Background(), taskID)
	if err != nil {
		if errors.Is(err, core.ErrManifestMissing) {
			return StartRunResult{Bootstrap: &BootstrapHint{Reason: "manifest_missing"}}, nil
		}
		return StartRunResult{}, err
	}
	view := toRunView(run)
	return StartRunResult{Run: &view}, nil
}
```

- [ ] **Step 2: Atualizar tests existentes em `runs_test.go`**

```bash
grep -n "RunsAPI.*Start\|api.Start\|res, err := .* Start(" internal/api/runs_test.go
```

Localize cada teste que chama `Start` e atualize a desconstrução pra:

```go
res, err := api.Start("t1")
if err != nil { t.Fatalf(...) }
if res.Run == nil { t.Fatal("expected run, got bootstrap hint") }
run := *res.Run
// continue assertions on run...
```

Adicione um teste novo:

```go
func TestRunsAPI_Start_ManifestMissingReturnsBootstrapHint(t *testing.T) {
	svc := &fakeRunsSvc{startErr: core.ErrManifestMissing}
	a := NewRunsAPI(svc, func() string { return "http://localhost:0" })
	res, err := a.Start("t1")
	if err != nil { t.Fatalf("err=%v, want nil (manifest_missing should not error)", err) }
	if res.Run != nil { t.Errorf("res.Run=%v, want nil", res.Run) }
	if res.Bootstrap == nil { t.Fatal("Bootstrap nil") }
	if res.Bootstrap.Reason != "manifest_missing" {
		t.Errorf("Reason=%q", res.Bootstrap.Reason)
	}
}
```

(Se `fakeRunsSvc` ainda não existe ou não suporta `startErr`, adicione.)

- [ ] **Step 3: Run**

```bash
go test -tags webkit2_41 ./internal/api/...
```

Expected: PASS (todos os antigos + o novo).

- [ ] **Step 4: Commit**

```bash
git add internal/api/runs.go internal/api/runs_test.go
git commit -m "$(cat <<'EOF'
refactor(F10.6.B.7.1): RunsAPI.Start returns StartRunResult{Run, Bootstrap}

ErrManifestMissing no longer surfaces as an error — instead it becomes
a typed hint (StartRunResult.Bootstrap = &BootstrapHint{Reason:
"manifest_missing"}). UI inspects which field is populated to decide
between showing the run or opening BootstrapModal.

This breaks the existing api.startRun JS callers — they'll be fixed in
Stage 10 along with the Wails binding regen.
EOF
)"
```

---

## Stage 8 — TasksService 4ª cleanup chain

### Task 8.1: TasksBootstrapCleanupFn + chamada em Patch/Discard

**Files:**
- Modify: `internal/core/tasks.go`
- Modify: `internal/core/tasks_test.go`

- [ ] **Step 1: Adicionar type + constructor arg**

Em `tasks.go`:

```go
type TasksBootstrapCleanupFn func(ctx context.Context, taskID string) error
```

Adiciona no struct:

```go
type TasksService struct {
	// ...
	bootstrapCleanup TasksBootstrapCleanupFn
}
```

Atualiza construtor:

```go
func NewTasksService(
	repo TasksRepoInterface, cat *catalog.Catalog, bus events.Emitter,
	worktreeCleanup TasksWorktreeCleanupFn,
	sessionCleanup TasksSessionCleanupFn,
	runsCleanup TasksRunsCleanupFn,
	bootstrapCleanup TasksBootstrapCleanupFn,
) *TasksService {
	return &TasksService{
		repo: repo, catalog: cat, bus: bus,
		worktreeCleanup:  worktreeCleanup,
		sessionCleanup:   sessionCleanup,
		runsCleanup:      runsCleanup,
		bootstrapCleanup: bootstrapCleanup,
	}
}
```

Adiciona helper + chama 1º em ambos os caminhos:

```go
func (s *TasksService) runBootstrapCleanup(ctx context.Context, taskID string) {
	if s.bootstrapCleanup == nil { return }
	if err := s.bootstrapCleanup(ctx, taskID); err != nil {
		log.Printf("bootstrap cleanup for task %s: %v", taskID, err)
	}
}
```

No `Patch` (depois da transição pra terminal):

```go
if IsTerminal(*in.State) {
	// Order: bootstrap → sessions → runs → worktrees.
	// Bootstrap first because it's the cheapest to kill (no containers,
	// no volumes, no network).
	s.runBootstrapCleanup(ctx, id)
	s.runSessionCleanup(ctx, id)
	s.runRunsCleanup(ctx, id)
	s.runWorktreeCleanup(ctx, id)
}
```

E em `Discard`:

```go
s.runBootstrapCleanup(ctx, id)
s.runSessionCleanup(ctx, id)
s.runRunsCleanup(ctx, id)
s.runWorktreeCleanup(ctx, id)
```

- [ ] **Step 2: Atualizar tests existentes (5+ ocorrências)**

Roda:

```bash
grep -n "NewTasksService" internal/ cmd/ main.go
```

Cada call site precisa de um arg a mais. Em `tasks_test.go`, adiciona `nil` ou o mock function.

- [ ] **Step 3: Adicionar teste de ordering**

```go
func TestTransition_TerminalRunsAllCleanupsInOrder_BootstrapFirst(t *testing.T) {
	var order []string
	mockBootstrap := func(_ context.Context, taskID string) error { order = append(order, "bootstrap:"+taskID); return nil }
	mockSession := func(_ context.Context, taskID string) error { order = append(order, "session:"+taskID); return nil }
	mockRuns := func(_ context.Context, taskID string) error { order = append(order, "runs:"+taskID); return nil }
	mockWorktree := func(_ context.Context, taskID string) error { order = append(order, "worktree:"+taskID); return nil }

	repo := &fakeTasksRepo{ /* seed task in "in_progress" state */ }
	cat := /* minimal catalog */
	svc := NewTasksService(repo, cat, &events.FakeEmitter{},
		mockWorktree, mockSession, mockRuns, mockBootstrap,
	)
	state := "done"
	if _, err := svc.Patch(context.Background(), "t1", PatchTaskInput{State: &state}); err != nil {
		t.Fatalf("Patch: %v", err)
	}
	want := []string{"bootstrap:t1", "session:t1", "runs:t1", "worktree:t1"}
	if !reflect.DeepEqual(order, want) {
		t.Errorf("order=%v, want %v", order, want)
	}
}

func TestTransition_BootstrapErrorDoesNotBlockOthers(t *testing.T) {
	calls := 0
	failing := func(_ context.Context, _ string) error { calls++; return errors.New("bootstrap fail") }
	other := func(_ context.Context, _ string) error { calls++; return nil }

	repo := &fakeTasksRepo{ /* seed */ }
	cat := /* minimal */
	svc := NewTasksService(repo, cat, &events.FakeEmitter{}, other, other, other, failing)
	state := "done"
	if _, err := svc.Patch(context.Background(), "t1", PatchTaskInput{State: &state}); err != nil {
		t.Fatalf("Patch: %v", err)
	}
	if calls != 4 {
		t.Errorf("calls=%d, want 4 (bootstrap failure must not block others)", calls)
	}
}
```

- [ ] **Step 4: Run**

```bash
go test -tags webkit2_41 ./internal/core/...
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add internal/core/tasks.go internal/core/tasks_test.go
git commit -m "$(cat <<'EOF'
feat(F10.6.B.8.1): TasksService gains bootstrap cleanup (4th in chain)

New order: bootstrap → sessions → runs → worktrees. Bootstrap first
because cheapest to kill. Each callback is tolerant (errors logged,
chain continues).

Two new tests assert ordering and tolerance.

Constructor signature changed — call sites in main.go, jarvis-e2e-http,
and existing tasks_test.go fixtures get one more arg (nil for tests
that don't exercise cleanup).
EOF
)"
```

---

## Stage 9 — Wiring (main.go + e2e-http)

### Task 9.1: main.go

**Files:**
- Modify: `main.go`

- [ ] **Step 1: Construir bootstrapSvc + bootstrapAPI; passar callback ao NewTasksService**

No `main.go`, entre as construções de `runsSvc` e `tasksSvc`:

```go
bootstrapSvc := core.NewBootstrapService(
	runtime, worktreesSvc, worktreesRepo, tasksRepo, catalogRoot, lazyBus,
)
```

Atualiza `NewTasksService` pra incluir o callback:

```go
tasksSvc := core.NewTasksService(tasksRepo, catalogRoot, lazyBus,
	worktreesSvc.CleanupForTask,
	sessionsSvc.CleanupForTask,
	runsSvc.CleanupForTask,
	bootstrapSvc.CleanupForTask,
)
```

E depois das outras `*API` constructions:

```go
bootstrapAPI := api.NewBootstrapAPI(bootstrapSvc)
```

No `Bind`:

```go
Bind: []any{
	app, healthAPI, tasksAPI, projectsAPI, masterAPI,
	worktreesAPI, sessionsAPI, catalogAPI, runsAPI,
	bootstrapAPI, // novo
},
```

- [ ] **Step 2: Compila smoke**

```bash
go build -tags webkit2_41 -o /tmp/jarvis-stage9 .
```

Expected: builds successfully (binary in `/tmp/jarvis-stage9`).

- [ ] **Step 3: Commit**

```bash
git add main.go
git commit -m "$(cat <<'EOF'
feat(F10.6.B.9.1): main.go wires BootstrapService + BootstrapAPI

bootstrapSvc constructed after runsSvc, before tasksSvc (so its
CleanupForTask can be passed in). bootstrapAPI added to the Wails
Bind list.
EOF
)"
```

### Task 9.2: cmd/jarvis-e2e-http/main.go

**Files:**
- Modify: `cmd/jarvis-e2e-http/main.go`

- [ ] **Step 1: Mirror the wiring**

Mesmas duas mudanças no e2e-http main: criar `bootstrapSvc`, passar callback ao `NewTasksService`. O e2e-http não precisa adicionar `bootstrapAPI` ao binding HTTP (não há suporte UI pra bootstrap no e2e shim hoje), mas o cleanup precisa pra paridade.

- [ ] **Step 2: Build smoke**

```bash
go build -tags 'e2e_http webkit2_41' -o /tmp/jarvis-e2e-stage9 ./cmd/jarvis-e2e-http
```

Expected: success.

- [ ] **Step 3: Commit**

```bash
git add cmd/jarvis-e2e-http/main.go
git commit -m "feat(F10.6.B.9.2): jarvis-e2e-http mirrors bootstrap wiring"
```

### Task 9.3: Regenerate Wails bindings

**Files:**
- Modify: `ui/src/wailsjs/go/api/BootstrapAPI.{d.ts,js}` (NEW)
- Modify: `ui/src/wailsjs/go/api/RunsAPI.{d.ts,js}` (regenerated for StartRunResult)

- [ ] **Step 1: Run wails generate**

```bash
wails generate module
```

> If `wails` is not on PATH, install:
> ```bash
> go install github.com/wailsapp/wails/v2/cmd/wails@latest
> export PATH="$HOME/go/bin:$PATH"
> ```

Expected: new files in `ui/src/wailsjs/go/api/BootstrapAPI.{d.ts,js}`; `RunsAPI.{d.ts,js}` updated to reflect `StartRunResult` return type from `Start`.

- [ ] **Step 2: Inspect generated files**

```bash
cat ui/src/wailsjs/go/api/BootstrapAPI.d.ts
diff <(git show HEAD~1:ui/src/wailsjs/go/api/RunsAPI.d.ts) ui/src/wailsjs/go/api/RunsAPI.d.ts
```

Confirm `Start(taskID: string): Promise<api.BootstrapView>` and `Cancel(taskID: string): Promise<void>` in `BootstrapAPI.d.ts`. Confirm `RunsAPI.Start` now returns `Promise<api.StartRunResult>`.

- [ ] **Step 3: Commit**

```bash
git add ui/src/wailsjs/go/api/
git commit -m "$(cat <<'EOF'
chore(F10.6.B.9.3): regenerate Wails bindings (BootstrapAPI + RunsAPI)

New BootstrapAPI binding. RunsAPI.Start now returns StartRunResult
(union of Run / Bootstrap).
EOF
)"
```

---

## Stage 10 — UI integration

### Task 10.1: Update `lib/events.ts` payload

**Files:**
- Modify: `ui/src/lib/events.ts`

- [ ] **Step 1: Edit `bootstrap.proposed` type**

Replace lines 77-80:

```typescript
  | {
      type: 'bootstrap.proposed'; session_id: ''; task_id: string;
      payload: {
        manifest_text: string;
        valid: boolean;
        errors: string[];
      };
      at: string;
    };
```

- [ ] **Step 2: Run UI tests (some will fail until we update consumers)**

```bash
cd ui && pnpm test --run --reporter=verbose lib/events
```

Type-check should pass; runtime tests may show payload mismatches downstream.

- [ ] **Step 3: Commit**

```bash
git add ui/src/lib/events.ts
git commit -m "feat(F10.6.B.10.1): extend bootstrap.proposed payload (manifest_text + valid + errors)"
```

### Task 10.2: Update `lib/api.ts`

**Files:**
- Modify: `ui/src/lib/api.ts`

- [ ] **Step 1: Import BootstrapBinding + add types**

Após os imports existentes:

```typescript
import * as BootstrapBinding from '../wailsjs/go/api/BootstrapAPI';
```

Adiciona após `Run` type:

```typescript
export type StartRunResult = {
  run: Run | null;
  bootstrap: { reason: string } | null;
};
```

- [ ] **Step 2: Update startRun + add bootstrapManifest real impl + add cancelBootstrap**

Substitui `startRun` (linha ~296):

```typescript
startRun: async (taskId: string): Promise<StartRunResult> => {
  const raw = await RunsBinding.Start(taskId);
  return {
    run: raw.run != null ? toRun(raw.run) : null,
    bootstrap: raw.bootstrap != null ? { reason: raw.bootstrap.reason } : null,
  };
},
```

Substitui `bootstrapManifest` (linha ~316):

```typescript
bootstrapManifest: async (taskId: string): Promise<BootstrapSession> => {
  const v = await BootstrapBinding.Start(taskId);
  return { session_id: v.session_id, cwd: v.cwd };
},
cancelBootstrap: (taskId: string): Promise<void> => BootstrapBinding.Cancel(taskId),
```

- [ ] **Step 3: TS check + tests**

```bash
cd ui && pnpm tsc --noEmit
pnpm test --run --reporter=verbose lib/api
```

Expected: tsc clean. Tests may need updating (Stage 10.4).

- [ ] **Step 4: Commit**

```bash
git add ui/src/lib/api.ts
git commit -m "$(cat <<'EOF'
feat(F10.6.B.10.2): api.ts real bootstrapManifest + StartRunResult

startRun now returns StartRunResult (union of run / bootstrap), matching
the new RunsAPI shape. bootstrapManifest stops being a notFound stub
and routes to BootstrapBinding.Start. New cancelBootstrap helper.
EOF
)"
```

### Task 10.3: Update `hooks/useRun.ts`

**Files:**
- Modify: `ui/src/hooks/useRun.ts`

- [ ] **Step 1: Edit useStartRun to surface bootstrap hint**

Para que componentes possam reagir:

```typescript
import { api, type Run, type StartRunResult } from '../lib/api';

export function useStartRun(taskId: string) {
  const qc = useQueryClient();
  return useMutation<StartRunResult, Error>({
    mutationFn: () => api.startRun(taskId),
    onSuccess: (result) => {
      if (result.run) {
        qc.invalidateQueries({ queryKey: queryKeys.run(taskId) });
      }
      // bootstrap branch handled by the caller — they inspect result.bootstrap
    },
  });
}
```

Adicione `useCancelBootstrap`:

```typescript
export function useCancelBootstrap(taskId: string) {
  return useMutation({ mutationFn: () => api.cancelBootstrap(taskId) });
}
```

- [ ] **Step 2: Update useRun.test.ts mocks at lines 77 + 108**

Line 77 mock muda de:

```typescript
(api.startRun as ReturnType<typeof vi.fn>).mockResolvedValue({ id: 'r1', ... });
```

Pra:

```typescript
(api.startRun as ReturnType<typeof vi.fn>).mockResolvedValue({
  run: { id: 'r1', task_id: 't1', /* ... */ },
  bootstrap: null,
});
```

Line 108 mock de `api.bootstrapManifest` muda de stub pra:

```typescript
(api.bootstrapManifest as ReturnType<typeof vi.fn>).mockResolvedValue({
  session_id: 's-1', cwd: '/tmp/wt',
});
```

Adicione um teste novo:

```typescript
it('useStartRun surfaces bootstrap hint when manifest missing', async () => {
  (api.startRun as ReturnType<typeof vi.fn>).mockResolvedValue({
    run: null,
    bootstrap: { reason: 'manifest_missing' },
  });
  // wrapper + render + call mutate; assert result.bootstrap.reason === 'manifest_missing'
});
```

- [ ] **Step 3: Run tests**

```bash
cd ui && pnpm test --run useRun
```

Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add ui/src/hooks/useRun.ts ui/src/hooks/useRun.test.ts
git commit -m "$(cat <<'EOF'
feat(F10.6.B.10.3): useStartRun returns StartRunResult; useCancelBootstrap

Mutation result now carries the bootstrap hint; the calling component
inspects result.bootstrap to decide whether to open BootstrapModal.
EOF
)"
```

### Task 10.4: Update `useSessionEvents.ts` handler

**Files:**
- Modify: `ui/src/hooks/useSessionEvents.ts`
- Modify: `ui/src/hooks/useSessionEvents.test.ts`

- [ ] **Step 1: Add an emitter param for bootstrap-proposed dispatch**

Adicione um novo callback opcional:

```typescript
export type BootstrapProposedEmitter = (payload: {
  task_id: string;
  manifest_text: string;
  valid: boolean;
  errors: string[];
}) => void;

export function useSessionEvents(
  queryClient: QueryClient,
  emitToast?: WorktreeOrphanedToastEmitter,
  emitBootstrapProposed?: BootstrapProposedEmitter,
): void {
  // ...
  'bootstrap.proposed': (e) => {
    if (emitBootstrapProposed) {
      emitBootstrapProposed({
        task_id: e.task_id,
        manifest_text: e.payload.manifest_text,
        valid: e.payload.valid,
        errors: e.payload.errors,
      });
    } else if (emitToast) {
      emitToast('Manifesto pronto. Abra o painel da tarefa.');
    }
  },
}
```

- [ ] **Step 2: Update useSessionEvents.test.ts**

Test em line 219 muda payload e asserções:

```typescript
it('bootstrap.proposed dispatches to bootstrap emitter when provided', () => {
  const emitBootstrap = vi.fn();
  // ... render hook with emitBootstrapProposed
  fireFakeWs({
    type: 'bootstrap.proposed', session_id: '', task_id: 't1',
    payload: { manifest_text: 'version: "1"\n', valid: true, errors: [] }, at: '...',
  });
  expect(emitBootstrap).toHaveBeenCalledWith(expect.objectContaining({
    task_id: 't1', valid: true, manifest_text: 'version: "1"\n', errors: [],
  }));
});
```

- [ ] **Step 3: Run tests**

```bash
cd ui && pnpm test --run useSessionEvents
```

Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add ui/src/hooks/useSessionEvents.ts ui/src/hooks/useSessionEvents.test.ts
git commit -m "$(cat <<'EOF'
feat(F10.6.B.10.4): useSessionEvents dispatches bootstrap.proposed to UI

New optional emitBootstrapProposed callback. Default: toast (preserves
old behavior when no callback wired). When wired, BootstrapModal phase 2
will subscribe to receive the manifest preview.
EOF
)"
```

### Task 10.5: BootstrapModal phase 2

**Files:**
- Modify: `ui/src/components/dialogs/BootstrapModal.tsx`
- Modify: `ui/src/components/dialogs/BootstrapModal.test.tsx`

- [ ] **Step 1: Rewrite BootstrapModal with phase state**

Substitui o `BootstrapModal.tsx` por uma versão com state machine: `idle → starting → waiting → preview`.

```typescript
import { useEffect, useRef, useState } from 'react';
import { useBootstrapManifest, useCancelBootstrap, useStartRun } from '../../hooks/useRun';
import {
  Dialog, DialogContent, DialogDescription, DialogHeader, DialogTitle,
} from '@/components/ui/dialog';

type Phase = 'idle' | 'starting' | 'waiting' | 'preview-valid' | 'preview-invalid';

type Props = {
  taskId: string;
  onClose: () => void;
  // External signal: parent subscribes to bootstrap.proposed via useSessionEvents
  // and forwards it here.
  proposed?: { manifest_text: string; valid: boolean; errors: string[] } | null;
};

const AUTO_FIRE_MS = 10_000;

export function BootstrapModal({ taskId, onClose, proposed }: Props) {
  const bootstrap = useBootstrapManifest(taskId);
  const cancel = useCancelBootstrap(taskId);
  const startRun = useStartRun(taskId);
  const [phase, setPhase] = useState<Phase>('idle');
  const [countdownMs, setCountdownMs] = useState(AUTO_FIRE_MS);
  const timerRef = useRef<number | null>(null);

  // Reflect external proposed prop into phase
  useEffect(() => {
    if (proposed == null) return;
    setPhase(proposed.valid ? 'preview-valid' : 'preview-invalid');
  }, [proposed]);

  // Countdown effect when in preview-valid
  useEffect(() => {
    if (phase !== 'preview-valid') return;
    setCountdownMs(AUTO_FIRE_MS);
    const start = Date.now();
    timerRef.current = window.setInterval(() => {
      const elapsed = Date.now() - start;
      const remaining = Math.max(0, AUTO_FIRE_MS - elapsed);
      setCountdownMs(remaining);
      if (remaining === 0) {
        if (timerRef.current) window.clearInterval(timerRef.current);
        startRun.mutate(undefined, {
          onSuccess: () => onClose(),
        });
      }
    }, 100);
    return () => {
      if (timerRef.current) window.clearInterval(timerRef.current);
    };
  }, [phase, startRun, onClose]);

  const onStart = () => {
    setPhase('starting');
    bootstrap.mutate(undefined, {
      onSuccess: () => setPhase('waiting'),
      onError: () => setPhase('idle'),
    });
  };

  const onCancelAll = () => {
    if (timerRef.current) window.clearInterval(timerRef.current);
    cancel.mutate(undefined, {
      onSettled: () => onClose(),
    });
  };

  const onRunNow = () => {
    if (timerRef.current) window.clearInterval(timerRef.current);
    startRun.mutate(undefined, {
      onSuccess: () => onClose(),
    });
  };

  return (
    <Dialog open onOpenChange={(o) => { if (!o) onCancelAll(); }}>
      <DialogContent
        className="bg-bg-surface border-border-subtle max-w-3xl"
        aria-label="bootstrap-manifest-modal"
      >
        <DialogHeader>
          <DialogTitle className="font-display text-text-emphasis">
            {phase === 'idle' || phase === 'starting' ? 'Manifesto faltando'
              : phase === 'waiting' ? 'Aguardando o Claude propor o manifesto'
              : phase === 'preview-valid' ? 'Manifesto pronto'
              : 'Manifesto inválido'}
          </DialogTitle>
          {(phase === 'idle' || phase === 'starting') && (
            <DialogDescription className="text-text-subtle">
              Esse projeto ainda não tem <code>.orchestrator/run.yml</code>. Clique em{' '}
              <strong>Iniciar bootstrap</strong> pra abrir uma sessão Claude efêmera que
              vai propor o manifesto.
            </DialogDescription>
          )}
        </DialogHeader>

        {(phase === 'idle' || phase === 'starting') && (
          <div className="flex gap-2 pt-2">
            <button type="button" onClick={onStart} disabled={bootstrap.isPending}>
              Iniciar bootstrap
            </button>
            <button type="button" onClick={onClose}>Cancelar</button>
          </div>
        )}

        {phase === 'waiting' && (
          <div className="flex gap-2 pt-2">
            <p className="text-text-subtle">Claude está lendo o repo. Quando salvar o manifesto, ele aparece aqui automaticamente.</p>
            <button type="button" onClick={onCancelAll}>Cancelar</button>
          </div>
        )}

        {(phase === 'preview-valid' || phase === 'preview-invalid') && proposed && (
          <>
            <pre className="bg-bg-elevated border border-border-subtle rounded p-2 overflow-auto max-h-96 text-xs">
              <code>{proposed.manifest_text}</code>
            </pre>
            {phase === 'preview-invalid' && (
              <ul className="text-status-error">
                {proposed.errors.map((e, i) => (<li key={i}>{e}</li>))}
              </ul>
            )}
            <div className="flex gap-2 pt-2 items-center">
              {phase === 'preview-valid' && (
                <button type="button" onClick={onRunNow}>
                  Run agora ({Math.ceil(countdownMs / 1000)}s)
                </button>
              )}
              <button type="button" onClick={onCancelAll}>Cancelar</button>
            </div>
          </>
        )}

        {bootstrap.isError && (
          <p role="alert">Falha ao iniciar bootstrap: {(bootstrap.error as Error).message}</p>
        )}
      </DialogContent>
    </Dialog>
  );
}
```

- [ ] **Step 2: Atualizar tests do BootstrapModal**

Adiciona casos:
- `phase=waiting` mostra "Cancelar" + spinner/texto
- `phase=preview-valid` com proposed: mostra `<pre>`, countdown, Run agora, auto-fires startRun em 10s (fake timers)
- `phase=preview-invalid`: mostra erros, sem countdown
- Cancel chama `api.cancelBootstrap` + onClose

Esqueleto:

```typescript
import { vi, describe, it, expect, beforeEach, afterEach } from 'vitest';
import { render, screen, fireEvent, act } from '@testing-library/react';
// ... existing setup ...

describe('BootstrapModal phase 2', () => {
  beforeEach(() => { vi.useFakeTimers(); });
  afterEach(() => { vi.useRealTimers(); });

  it('shows preview + countdown when proposed valid', () => {
    render(<BootstrapModal taskId="t1" onClose={vi.fn()} proposed={{
      manifest_text: 'version: "1"\nservices: {}\n', valid: true, errors: [],
    }} />);
    expect(screen.getByText(/Manifesto pronto/)).toBeInTheDocument();
    expect(screen.getByText(/Run agora/)).toBeInTheDocument();
  });

  it('auto-fires startRun after 10s', async () => {
    (api.startRun as ReturnType<typeof vi.fn>).mockResolvedValue({ run: { id: 'r1' }, bootstrap: null });
    const onClose = vi.fn();
    render(<BootstrapModal taskId="t1" onClose={onClose} proposed={{
      manifest_text: '...', valid: true, errors: [],
    }} />);
    await act(async () => { vi.advanceTimersByTime(10_000); });
    expect(api.startRun).toHaveBeenCalledWith('t1');
  });

  it('preview-invalid shows errors and no countdown', () => {
    render(<BootstrapModal taskId="t1" onClose={vi.fn()} proposed={{
      manifest_text: 'bad', valid: false, errors: ['missing version'],
    }} />);
    expect(screen.getByText('missing version')).toBeInTheDocument();
    expect(screen.queryByText(/Run agora/)).toBeNull();
  });

  it('Cancel button calls cancelBootstrap + onClose', async () => {
    (api.cancelBootstrap as ReturnType<typeof vi.fn>).mockResolvedValue(undefined);
    const onClose = vi.fn();
    render(<BootstrapModal taskId="t1" onClose={onClose} proposed={{ manifest_text: '...', valid: true, errors: [] }} />);
    fireEvent.click(screen.getByText('Cancelar'));
    await vi.waitFor(() => expect(api.cancelBootstrap).toHaveBeenCalledWith('t1'));
    expect(onClose).toHaveBeenCalled();
  });
});
```

> **Note:** the existing tests at the top of `BootstrapModal.test.tsx` (phase 1 — "Iniciar bootstrap") still need to compile and pass. Update mocks of `api.bootstrapManifest` from `notFound` to the real shape `{session_id, cwd}`.

- [ ] **Step 3: Run tests**

```bash
cd ui && pnpm test --run BootstrapModal
```

Expected: PASS (both phase 1 retained + phase 2 added).

- [ ] **Step 4: Commit**

```bash
git add ui/src/components/dialogs/BootstrapModal.tsx ui/src/components/dialogs/BootstrapModal.test.tsx
git commit -m "$(cat <<'EOF'
feat(F10.6.B.10.5): BootstrapModal phase 2 (preview + countdown + Run/Cancel)

State machine: idle → starting → waiting → preview-valid | preview-invalid.
In preview-valid, a 10s countdown auto-fires api.startRun via useStartRun.
preview-invalid shows the YAML and the validation errors but no
countdown — user keeps talking to Claude to fix it; the next valid
emit transitions back to preview-valid.

Parent wires `proposed` prop from useSessionEvents' bootstrap.proposed
handler (Stage 10.6 below).
EOF
)"
```

### Task 10.6: Wire parent to forward proposed prop

**Files:**
- Modify: o componente que renderiza `BootstrapModal` (provavelmente `TaskDetailSheet.tsx` ou `RunStatus.tsx`)
- Modify: `useSessionEvents` consumer no nível mais alto pra capturar e expor o último `bootstrap.proposed`

- [ ] **Step 1: Find current renderer**

```bash
grep -rn "BootstrapModal" ui/src --include='*.tsx' | grep -v test
```

- [ ] **Step 2: Add zustand store for last-proposed**

Cria `ui/src/stores/bootstrapProposed.ts`:

```typescript
import { create } from 'zustand';

type Payload = { task_id: string; manifest_text: string; valid: boolean; errors: string[] };

type State = {
  last: Payload | null;
  setLast: (p: Payload | null) => void;
};

export const useBootstrapProposedStore = create<State>((set) => ({
  last: null,
  setLast: (p) => set({ last: p }),
}));
```

No componente que monta `useSessionEvents` (provavelmente `App.tsx` ou root):

```typescript
const setLast = useBootstrapProposedStore((s) => s.setLast);
useSessionEvents(queryClient, emitToast, setLast);
```

No componente que renderiza `BootstrapModal`:

```typescript
const last = useBootstrapProposedStore((s) => s.last);
const proposedForTask = last?.task_id === taskId ? {
  manifest_text: last.manifest_text, valid: last.valid, errors: last.errors,
} : null;
// ...
<BootstrapModal taskId={taskId} onClose={onClose} proposed={proposedForTask} />
```

- [ ] **Step 3: Update consumer hooks integration tests**

Garanta que os tests de `useStartRun` consumer (provavelmente `RunStatus.test.tsx` ou `TaskDetailSheet.test.tsx`) cobrem o branch `result.bootstrap != null` → abre modal.

```typescript
it('opens BootstrapModal when startRun returns bootstrap hint', async () => {
  (api.startRun as ReturnType<typeof vi.fn>).mockResolvedValue({ run: null, bootstrap: { reason: 'manifest_missing' } });
  // render TaskDetailSheet with a task
  fireEvent.click(screen.getByText(/▶ Run/));
  await vi.waitFor(() => expect(screen.getByLabelText('bootstrap-manifest-modal')).toBeInTheDocument());
});
```

- [ ] **Step 4: Run all UI tests**

```bash
cd ui && pnpm test
```

Expected: PASS (~400 testes).

- [ ] **Step 5: Commit**

```bash
git add ui/src/
git commit -m "$(cat <<'EOF'
feat(F10.6.B.10.6): wire bootstrap.proposed → BootstrapModal preview

New zustand store bootstrapProposedStore caches the last
bootstrap.proposed payload. useSessionEvents writes to it via the new
emitBootstrapProposed callback. BootstrapModal reads from the store
filtered by taskId so each modal only sees its own task's payload.

useStartRun consumer (TaskDetailSheet/RunStatus) now opens the modal
when result.bootstrap is populated.
EOF
)"
```

---

## Stage 11 — Integration smoke + final verification

### Task 11.1: //go:build integration smoke

**Files:**
- Create: `internal/core/bootstrap_integration_test.go`

- [ ] **Step 1: Write the smoke**

```go
//go:build integration

package core

import (
	"context"
	"os"
	"path/filepath"
	"testing"
	"time"

	"github.com/marcosdid/jarvis/internal/sandbox"
)

func TestBootstrap_Smoke_RealAijail(t *testing.T) {
	if _, err := sandbox.DetectTerminal(); err != nil {
		t.Skipf("no terminal: %v", err)
	}
	if err := sandbox.SandboxAvailable(); err != nil {
		t.Skipf("sandbox not available: %v", err)
	}

	env := newBootstrapTestEnv(t)
	defer env.cleanup()
	// Swap runtime to a real one
	env.svc.runtime = sandbox.NewAijailRuntime()

	ctx := context.Background()
	started, err := env.svc.Start(ctx, env.taskID)
	if err != nil { t.Fatalf("Start: %v", err) }
	<-started.WatcherReady

	defer func() { _ = env.svc.Cancel(ctx, env.taskID) }()

	// Drop a valid manifest from the test process (simulates Claude writing).
	manifest := []byte(`version: "1"
services:
  web:
    image: nginx
`)
	if err := os.WriteFile(started.ManifestPath, manifest, 0o644); err != nil {
		t.Fatalf("write manifest: %v", err)
	}

	deadline := time.Now().Add(5 * time.Second)
	for time.Now().Before(deadline) {
		for _, c := range env.emitter.Snapshot() {
			if c.Name == "bootstrap.proposed" {
				p := c.Payload.(BootstrapProposedPayload)
				if p.Valid {
					return // success
				}
			}
		}
		time.Sleep(50 * time.Millisecond)
	}
	t.Fatal("bootstrap.proposed valid never emitted in 5s (real ai-jail)")
}
```

- [ ] **Step 2: Run integration**

```bash
go test -tags 'integration webkit2_41' -run TestBootstrap_Smoke -v ./internal/core/...
```

Expected: PASS if ai-jail is on PATH; SKIP otherwise.

- [ ] **Step 3: Commit**

```bash
git add internal/core/bootstrap_integration_test.go
git commit -m "$(cat <<'EOF'
test(F10.6.B.11.1): integration smoke for bootstrap with real ai-jail

//go:build integration — only runs when sandbox + terminal are
available. Writes the manifest from the test process (simulating
Claude) and asserts the event fires within 5s. Useful as a CI smoke
when wired to a runner that has ai-jail installed.
EOF
)"
```

### Task 11.2: Final verification

- [ ] **Step 1: go vet across all tags**

```bash
go vet -tags webkit2_41 ./...
go vet -tags 'webkit2_41 e2e_http' ./...
go vet -tags 'webkit2_41 integration' ./...
```

Expected: clean.

- [ ] **Step 2: Test under race detector (core packages)**

```bash
go test -tags webkit2_41 -race -count=1 ./internal/core/... ./internal/sandbox/... ./internal/store/... ./internal/api/...
```

Expected: PASS, no race warnings.

- [ ] **Step 3: gofmt check**

```bash
gofmt -l .
```

Expected: empty output. If files listed, run `gofmt -w <files>` and commit.

- [ ] **Step 4: Build smoke binary**

```bash
go build -tags webkit2_41 -o /tmp/jarvis-f106b-final .
ls -la /tmp/jarvis-f106b-final
```

Expected: binary ~19MB+.

- [ ] **Step 5: Full UI test suite**

```bash
cd ui && pnpm test --run
```

Expected: 397+ PASS.

- [ ] **Step 6: Stage any gofmt fixes (if any) and commit**

```bash
git add -A
git commit -m "chore(F10.6.B.11.2): final verification (vet/race/gofmt clean)" || echo "nothing to commit"
```

---

## Stage 12 — (Optional) ADR

### Task 12.1: ADR-0035 — bootstrap as ephemeral runtime spawn

**Files:**
- Create: `docs/adr/ADR-0035-bootstrap-ephemeral-spawn.md`

- [ ] **Step 1: Write the ADR (~15 lines)**

```markdown
# ADR-0035: Bootstrap as ephemeral runtime spawn (no DB row)

**Status:** Accepted (F10.6 spec #2, 2026-05-16)

**Context:** When ▶ Run finds no `.orchestrator/run.yml`, we need a way for the
user to generate one. Options considered:

1. Extend `sessions` table with `kind='bootstrap'` flag, reuse SessionsService.
2. Inline spawn inside RunsService.StartRun on `ErrManifestMissing`.
3. **Dedicated `BootstrapService` with in-memory map (no DB)** — chosen.

**Decision:** Option 3. The bootstrap is a one-shot wizard with no persistent
state worth keeping — there are no transcripts to query, no replay semantics,
no `--resume`. A DB row would be a liability (orphans after crashes, schema
migrations, FK to tasks that may be discarded). The in-memory map keyed by
`task_id` is reset on app restart; the user can re-trigger if they wanted to
keep going.

**Consequences:**
- Crash recovery is "best-effort manual" — Claude bootstrap PID survives
  detached; user closes the terminal. Documented in the spec as accepted.
- Test surface stays minimal — no DB seed for bootstrap-specific cases.
- Idempotency handled by map check under `sync.Mutex`, not by DB unique index.
```

- [ ] **Step 2: Commit**

```bash
git add docs/adr/ADR-0035-bootstrap-ephemeral-spawn.md
git commit -m "docs(F10.6.B.12.1): ADR-0035 bootstrap as ephemeral runtime spawn"
```

---

## Done criteria

- [x] All Stage 1-11 tasks marked complete
- [x] `go vet -tags webkit2_41 ./...` clean
- [x] `go test -tags webkit2_41 -race ./internal/...` PASS
- [x] `cd ui && pnpm test --run` PASS
- [x] Binary builds via `go build -tags webkit2_41 .`
- [x] `gofmt -l .` empty
- [x] (Optional) ADR-0035 committed
- [ ] User runs `git push -u origin feat/f10.5-catalog-mcp` themselves
