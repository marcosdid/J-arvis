# F10.8 spec #1 — Python Cleanup (design)

> **Status:** design (brainstorm 2026-05-18). Primeira de duas specs do bloco
> F10.8 (final do roadmap F10). Esta cobre: deletar todo o código Python do
> repo (daemon `orchestrator/`, testes `pytest`, `alembic/`, deps Python),
> renomear `Makefile.f10` → `Makefile`, e rewrite das docs `ARCHITECTURE.md`
> + `PRODUCT.md` pra refletir a realidade Go+Wails. Spec #2 (próxima) tratará
> packaging (.deb + .AppImage + assets desktop).

## Problema

O J-arvis começou como Python+FastAPI (até F9). A pivotação pra Go+Wails
(F10) substituiu tudo — daemon, store, sandbox runtime, API layer, hooks
— mas o código antigo permaneceu no repo. Resultado:

- Quem clona o repo hoje vê `orchestrator/`, `pyproject.toml`, `tests/`
  e razoavelmente pensa que é um projeto Python. `main.go` é o
  entrypoint real.
- `ARCHITECTURE.md` linha 25 ainda descreve "Orchestrator daemon (Python
  + FastAPI)". `PRODUCT.md` linha 30 lista "Daemon: Python 3.13 + FastAPI
  + SQLAlchemy". Docs vivos mentem.
- 2 Makefiles coexistem (`Makefile` Python-era + `Makefile.f10` Go).
  Onboarding de novo dev cai no Makefile errado.
- ~3000 LOC de Python pesando reviews + `git grep` ruidoso.

Spec #1 limpa tudo isso em 3 commits sequenciais (cada um deixa o repo
num estado consistente).

## Não-objetivos

- Packaging (.deb, .AppImage) — vai pro spec #2.
- CI/CD workflows — não existem em `.github/`, fora do MVP.
- Rewrite de specs/plans antigos em `docs/superpowers/` — registro
  histórico, devem permanecer.
- Rewrite de `CHANGELOG.md` — registro histórico legítimo.
- ADR novo documentando "removemos Python" — o racional do pivot está
  em `docs/superpowers/specs/2026-05-12-pivot-go-wails-native-design.md`
  (spec dedicado, não um ADR). Pra discoverability via
  `docs/adr/README.md`, considerar adicionar uma linha lá apontando pro
  spec — mas isso é polish, não bloqueia este spec.
- Branch/tag arquivando o último estado Python — `git log --all
  --full-history -- orchestrator/` recupera quando precisar.

## Decisões de design (do brainstorm)

| Pergunta | Resposta |
|---|---|
| Estratégia do Makefile | **Renomear** `Makefile.f10 → Makefile`. Deleta o antigo (Python-era). User volta a usar `make start` em vez de `make -f Makefile.f10 start` |
| Doc updates (ARCHITECTURE/PRODUCT) | **Reescrever neste spec**. Não substitui por placeholder — escreve a doc Go+Wails de verdade |
| Preservação histórica do código Python | **Nada explícito** — git log basta. `git checkout <pre-cleanup-sha> -- orchestrator/` recupera quando precisar |
| `.orchestrator/data/` no .gitignore | **Remover** — era da era Python. Go app usa SQLite em `~/.local/share/jarvis/`, manifest em `.orchestrator/run.yml` (commitado, não dado) |

## Arquitetura: 3 commits sequenciais

Cada commit deixa o repo num estado consistente (build + tests verdes).

```
┌─ Commit 1: chore(F10.8.C.1): delete Python orchestrator ─────────────────┐
│ • git rm -r orchestrator/ tests/ alembic/                                │
│ • git rm alembic.ini pyproject.toml uv.lock                              │
│ • git rm Dockerfile.orchestrator .dockerignore                           │
│ • git rm scripts/f8-followups.sh scripts/f8-merge.sh scripts/f8-push.sh  │
│ • Modify .gitignore (remove 9 Python-only entries)                       │
│ • Verify: go vet + go build + go test all green                          │
└──────────────────────────────────────────────────────────────────────────┘
                              ↓
┌─ Commit 2: chore(F10.8.C.2): rename Makefile.f10 → Makefile ─────────────┐
│ • git rm Makefile  (the old Python-era one)                              │
│ • git mv Makefile.f10 Makefile                                           │
│ • grep -r "Makefile.f10" --include='*.md' . in live docs (README,        │
│   CONTEXT.md, gotchas.md) → substitute by "Makefile". Historical specs/  │
│   plans NOT modified (record of when that was reality).                  │
│ • Verify: make start --dry-run works                                     │
└──────────────────────────────────────────────────────────────────────────┘
                              ↓
┌─ Commit 3: docs(F10.8.C.3): rewrite ARCHITECTURE.md + PRODUCT.md ────────┐
│ • ARCHITECTURE.md: rewrite topology diagram, dir tree, test stack        │
│   section. Replace Python+FastAPI references with Go+Wails reality.      │
│ • PRODUCT.md: rewrite "Stack técnica resumida" section + scan remaining  │
│   11 mentions of Python/FastAPI/orchestrator+/uvicorn/alembic/sqlalchemy │
│   for contextual replacement.                                            │
│ • Verify: grep -ic "python\|fastapi" ARCHITECTURE.md PRODUCT.md ≈ 0      │
└──────────────────────────────────────────────────────────────────────────┘
```

Por que 3 commits separados (vs 1 big delete):

- Commit 1 é destrutivo e voluminoso (~3000 LOC removidas). Diff óbvio
  ("tudo daquele subdir foi").
- Commit 2 é renomeação pura — git rename detection preserva
  history; review é trivial.
- Commit 3 é doc prosa — revisão de prosa separada do code review.

## Inventário exato

### Commit 1: arquivos pra deletar (tracked)

| Path | O que era |
|---|---|
| `orchestrator/` | Daemon FastAPI: `api/`, `config/`, `core/`, `events/`, `hooks/`, `mcp/`, `notifications/`, `planner/`, `runtime/`, `sandbox/`, `store/`, `__init__.py`, `main.py` |
| `tests/` | pytest suite: `unit/`, `integration/`, `e2e/`, `conftest.py`, `__init__.py` |
| `alembic/` | Migrations Python: `env.py`, `script.py.mako`, `versions/` |
| `alembic.ini` | Alembic config |
| `pyproject.toml` | Python deps + ruff/pytest/mypy config |
| `uv.lock` | uv lockfile |
| `Dockerfile.orchestrator` | Container build do daemon Python |
| `.dockerignore` | Servia o Dockerfile.orchestrator (único Dockerfile) |
| `scripts/f8-followups.sh` | Era F8 (master session, Python) |
| `scripts/f8-merge.sh` | Idem |
| `scripts/f8-push.sh` | Idem |
| `scripts/README.md` | Documenta os 3 scripts acima. Sem eles vira doc dangling — deletar junto |

### Commit 1: arquivos pra limpar do disco (não tracked, gitignored)

| Path | Ação |
|---|---|
| `orchestrator/__pycache__/`, `tests/__pycache__/`, `alembic/__pycache__/` | `rm -rf` |
| `.pytest_cache/`, `.ruff_cache/`, `.mypy_cache/`, `.venv/` | `rm -rf` |
| `.coverage`, `htmlcov/` | `rm -rf` |

Estes não viram commits — só limpam o disco. Confirma via `git status`
que ficam fora do tracking.

### Commit 1: `.gitignore` diff

Remover entries Python-only:

```diff
- __pycache__/
- *.py[cod]
- .pytest_cache/
- .mypy_cache/
- .ruff_cache/
- .venv/
- .python-version
- uv.lock.diff
- .orchestrator/data/
```

Mantém: Go-related (`/jarvis`, `/jarvis-e2e-http`, `coverage.out`,
`build/`), Node (`node_modules/`), Wails (`build/bin/`).

### Commit 2: Makefile rename

```bash
git rm Makefile          # old (refs uv, pytest, alembic, orchestrator)
git mv Makefile.f10 Makefile
```

Substituir `Makefile.f10` por `Makefile` em **docs vivos** apenas:

- `README.md` (se mencionar build command)
- `CONTEXT.md` (se exists)
- `gotchas.md` (se exists)

**NÃO substituir** em:
- `docs/superpowers/specs/*.md` — registro histórico
- `docs/superpowers/plans/*.md` — idem
- `CHANGELOG.md` — idem
- `docs/adr/*.md` — registro arquitetônico imutável

### Commit 3: ARCHITECTURE.md rewrite

Inventário completo (line numbers exatos, verificados via grep):

| Linha | Conteúdo atual | Ação |
|---|---|---|
| 25 | `│  Orchestrator daemon (Python + FastAPI) — fora da jaula     │` (diagrama ASCII topology) | Reescrever: binário Wails (Go) com WebView webkit2gtk-4.1 embedando UI React; SQLite local via modernc.org/sqlite; ai-jail externo gerenciando sessões Claude isoladas |
| 42-56 | `ClaudeSession` SQLAlchemy/AsyncSession references | Reescrever: `internal/core/sessions.go` + `store.Session` (sqlite3 via modernc.org/sqlite, sem ORM) |
| 101 | `Manifesto .orchestrator/run.yml no repo (commitado, parte do projeto)` | **KEEP** — runtime path legítimo, usado por Go core (`internal/core/bootstrap.go`, `internal/core/runs.go`). NÃO pattern-deletar |
| 113-142 | Directory tree completa com `pyproject.toml`, `orchestrator/`, `alembic/`, `tests/unit/pytest`, `tests/integration/pytest+testcontainers`, `.orchestrator/data/` | Substituir tree inteira pelo layout real: `cmd/jarvis-e2e-http/`, `internal/{api,catalog,core,events,git,hooks,localhttp,master,mcp,osintegration,sandbox,store}/`, `ui/`, `main.go`, `app.go`, `Makefile`, `wails.json`, `go.mod`, `go.sum`. Path `.orchestrator/run.yml` continua mencionado como manifest do task |
| 159-161 | Test stack: "Unit (Python) pytest", "Integration httpx+testcontainers", "E2E Playwright+testcontainers-python" | Reescrever: "Unit + Integration (Go): `go test -tags webkit2_41 ./internal/...` com `//go:build integration` pra integração com Docker; UI (TS): `cd ui && pnpm test` via vitest; E2E (TS): Playwright contra `cmd/jarvis-e2e-http` (HTTP shim)" |
| 164-166 | `# pragma: no cover` rationale + `Protocol` + `match`/`case` references | Reescrever: cobertura Go via `go test -cover` (não 100% rigorous como pytest era; coverage de unit + integration); UI cobertura via vitest defaults; sem equivalent a `# pragma: no cover` (Go não tem) |
| 176-194 | Bloco `python` code fence com `class SessionRuntime(Protocol)`, `class ProcessSpawner(Protocol)`, `class DockerRuntime(Protocol)`, `class Clock(Protocol)`, `class HookSink(Protocol)` | Substituir por equivalente Go: interfaces `sandbox.Runtime`, `sandbox.DockerOps`, `events.Emitter` (mostradas em `internal/sandbox/runtime.go`, `internal/sandbox/docker_ops.go`, `internal/events/bus.go`) |
| 207-216 | Phase table F0-F7 ✅ com células mencionando "pyproject, FastAPI scaffold, Pydantic manifest parser, `orchestrator/config/catalog.yml`" | Estender a tabela com 4 novas linhas: F8 (master Claude sidebar), F9 (UI redesign CIPHER), F10 (Go+Wails pivot — overview), F10.7 (OS integration). Re-rotular células F6/F7 substituindo "Pydantic" por "yaml.v3 + Validate", "`orchestrator/config/catalog.yml`" por "`internal/catalog/catalog.yml`" |
| 218 | "MVP = F0 → F7" | Atualizar pra "MVP = F0 → F10" |
| 222-227 | DoD com `make test-all`, `# pragma: no cover` references | Substituir por: `make test` (gofmt + go vet + go test + ui test), `make lint` (gofmt + golangci-lint) |
| 239 | ADR-0002 row: "Python + daemon — 3.13 + FastAPI + SQLAlchemy 2 async + Alembic" | Atualizar pra refletir que ADR-0002 ainda existe mas a stack mudou. Adicionar linha apontando pra `docs/superpowers/specs/2026-05-12-pivot-go-wails-native-design.md` (spec que documenta o pivot) |
| 243, 257-259 | ADR rows referencing manifest/bootstrap/catalog features | **KEEP texto, verificar paths** — features ainda existem em Go (`internal/core/manifest.go`, `internal/core/bootstrap.go`, `internal/catalog/`) |

**Decisão sobre a phase table F0-F10** (advisory do reviewer): estender
a tabela existente com 4 novas linhas. Alternativa "replace entirely com
banner 'pivoted in F10'" foi descartada — a fase table é informativa e
F0-F7 são history legítima.

### Commit 3: PRODUCT.md rewrite

Inventário completo (line numbers exatos):

| Linha | Conteúdo atual | Ação |
|---|---|---|
| 17-25 | Seção "Stack técnica resumida" — bloco bullet com "Daemon: Python 3.13 + FastAPI + SQLAlchemy 2 async + Alembic + SQLite local" | Reescrever bloco inteiro (vide detalhamento abaixo) |
| 333 | §9.1 "Arquitetura em uma frase" — "Daemon Python fora da jaula, sessões Claude dentro, UI no browser, SQLite local, comunicação por hooks (Claude → daemon) e WebSocket (daemon → UI)" | Substituir: "Binário Wails (Go) com WebView embedando UI React, ai-jail isolando sessões Claude, SQLite local, comunicação UI ↔ binário via Wails bindings in-process, Claude → binário via hooks HTTP loopback" |
| 350 | §9.2 "ClaudeSession (tabela `sessions`; classe renomeada pra não colidir com SQLAlchemy)" | Atualizar: "Session (tabela `sessions`; struct `store.Session`)". Razão da renomeação histórica deixa de existir |
| 368 | §9.3 "ADR-0004 — TDD iron-law, 100% cobertura. Três camadas (unit Python, integration de rotas, E2E Playwright)" | Atualizar: "ADR-0004 — TDD discipline. Camadas: unit + integration Go (`go test`), UI vitest, E2E Playwright contra `cmd/jarvis-e2e-http`. Coverage rigor 100% deprecado durante o pivot — alvo é coverage saudável sem metric-gaming" |
| 379-389 | §9.4 Roadmap table com colunas F0-F8, célula "MVP = F0 → F7" | Estender com F9, F10.x rows. Atualizar "MVP = F0 → F10" |
| 396 | §9.5 "Costuras explícitas via Protocol (`SessionRuntime`, `ProcessSpawner`, `DockerRuntime`, `Clock`, `HookSink`)" | Substituir: "Costuras explícitas via interfaces Go (`sandbox.Runtime`, `sandbox.DockerOps`, `events.Emitter`, `git.Ops`)" |
| 397 | §9.5 "`# pragma: no cover` admitido para linhas defensivas..." | Remover linha (Go não tem equivalente; cobertura conduzida por `go test -cover` sem exclusões formais) |
| 402-430 | §9.6 Directory tree com `pyproject.toml`, `orchestrator/`, full Python layout, `tests/{unit,integration,e2e}`, `alembic/` | Substituir pelo tree real Go (mesmo que em ARCHITECTURE.md L113-142 — manter consistência cross-doc) |
| 447 | §9.8 "Orquestrador lê o manifesto `.orchestrator/run.yml`" | **KEEP** — runtime convention legítimo |
| 456 | §10 TL;DR — "Python 3.13 + FastAPI + SQLite + SQLAlchemy 2 async no backend... MVP em 8 fases (F0→F7), atualmente em F5" | Reescrever inteiro: "Binário Wails (Go 1.26) + UI React + SQLite local. TDD com interfaces Go pra costuras. Sandbox via ai-jail externo, com `sandbox.Runtime` substituível. MVP completo em F10.8 (Go+Wails native)" |

Substituição do bloco "Stack técnica resumida" (linhas 17-25), de:

```
- Daemon: Python 3.13 + FastAPI + SQLAlchemy 2 async + Alembic + SQLite local.
- UI: Vite 6 + React 19 + TanStack Query + Zustand + @dnd-kit (drag & drop).
- Sandbox: ai-jail externo (Fabio Akita) — bwrap + Landlock + seccomp.
- Comunicação Claude Code ↔ daemon: hooks (Notification, Stop, PreToolUse)
  escritos em `<worktree>/.claude/settings.json` antes do `ai-jail run`.
- Comunicação daemon ↔ UI: REST + WebSocket único em `/ws` com envelope tipado.
- Memória entre sessões: delegada ao `claude-mem` — o orquestrador não
  duplica essa responsabilidade.
- Plataforma: Linux only no MVP.
```

Para:

```
- App: binário único Go 1.26 + Wails v2.12 (WebView webkit2gtk-4.1
  embedando UI React em produção) + SQLite via modernc.org/sqlite + goose
  migrations.
- UI: React 19 + Tailwind v4 + shadcn/ui + TanStack Query + Zustand +
  @dnd-kit (drag & drop). Compilada em estático (Vite) e embedada no
  binário Go via //go:embed.
- Sandbox: ai-jail externo (Fabio Akita) — bwrap + Landlock + seccomp.
- Comunicação Claude Code ↔ app: hooks (Notification, Stop, PreToolUse)
  escritos em `<worktree>/.claude/settings.json` antes do `ai-jail run`;
  hooks respondem via HTTP loopback `127.0.0.1:N` montado pelo binário.
- Comunicação UI ↔ app: Wails bindings in-process (chamadas Go → JS
  diretas via runtime do WebView; sem HTTP/WS).
- OS integration: system tray (fyne.io/systray), single-instance
  D-Bus (Wails SingleInstanceLock), close-to-tray lifecycle, CLI
  `--focus` flag bindável via Super+J no DE.
- Memória entre sessões: delegada ao `claude-mem` — o orquestrador não
  duplica essa responsabilidade.
- Plataforma: Linux only no MVP (Ubuntu 22.04+ Wayland ou X11).
```

Tabela acima cobre as 7 seções estruturais (§9.1, §9.2, §9.3, §9.4,
§9.5, §9.6, §10) que precisam rewrite — não é só grep de keyword. A
contaminação é estrutural (frases inteiras, code blocks, tabelas de
roadmap), não apenas menções pontuais a "Python" ou "FastAPI".

## Distinção crítica: `orchestrator/` (delete) vs `.orchestrator/` (keep)

**`orchestrator/`** (sem ponto inicial) — diretório com código Python
do daemon FastAPI. **Deletar inteiramente** no Commit 1.

**`.orchestrator/`** (com ponto inicial) — convenção runtime do projeto.
Cada task tem um `.orchestrator/run.yml` no seu worktree (manifest
docker-compose-like processado por `internal/core/manifest.go` e
`internal/core/runs.go`). **Manter** — é parte ativa do contrato F10.6
(Run from Panel).

Verificação rápida:

```bash
# Diretório obsoleto:
ls orchestrator/ 2>/dev/null  # após Commit 1: "No such file or directory"

# Convenção runtime (qualquer task com manifest tem isto):
find . -name 'run.yml' -path '*/.orchestrator/*' 2>/dev/null
# Pode estar vazio se nenhum task tem manifest criado, mas o CONTRATO
# (paths usados em Go code) continua válido.
```

## Tratamento de erros / verificação

### Após Commit 1

```bash
go vet -tags webkit2_41 ./...
go test -tags webkit2_41 -race ./internal/...
go build -tags webkit2_41 -o /tmp/jarvis-cleanup .
```

Expected: vet limpo, testes Go passam, binário ~24MB. Erro indica que
algum import Go tinha referência fantasma ao Python (não deveria —
F10.5/6/7 já provaram que main.go é 100% Go).

Smoke check do runtime path (importante — distingue diretório `orchestrator/`
deletado vs convenção `.orchestrator/run.yml` mantida):

```bash
grep -rn '\.orchestrator/run\.yml' internal/ 2>/dev/null | head -5
# Expected: 4+ matches (bootstrap.go, runs.go, manifest.go + tests).
# Convenção continua válida mesmo após apagar o dir orchestrator/.
```

### Após Commit 2

```bash
ls Makefile Makefile.f10 2>&1   # Makefile exists; Makefile.f10 not found
make start --dry-run | head -5  # mostra targets Go
grep -r "Makefile.f10" --include='*.md' README.md CONTEXT.md gotchas.md 2>/dev/null
# Expected: 0 ocorrências em docs vivos
```

### Após Commit 3

Grep precisa distinguir entre **diretório obsoleto `orchestrator/`** e
**convenção runtime `.orchestrator/run.yml`** (que continua válida).
Padrão simples `orchestrator/` matcha ambos — usar 2 greps:

```bash
# 1) Menções "Python", "FastAPI", "Pydantic", "Alembic", "SQLAlchemy" — devem ir a 0
grep -ic 'python\|fastapi\|pydantic\|alembic\|sqlalchemy\|uvicorn' ARCHITECTURE.md PRODUCT.md
# Expected: 0 (ou 1-2 menções históricas legítimas como "stack pivoted in F10")

# 2) Menções a `orchestrator/` (diretório deletado) excluindo `.orchestrator/` (path mantido)
grep -n 'orchestrator/' ARCHITECTURE.md PRODUCT.md | grep -v '\.orchestrator/'
# Expected: 0 matches (qualquer match = ref ao diretório deletado, deve ser removido)

# 3) Menções a `.orchestrator/run.yml` (KEEP)
grep -c '\.orchestrator/run\.yml' ARCHITECTURE.md PRODUCT.md
# Expected: 1+ em cada arquivo (runtime convention documentation)
```

### Smoke manual (opcional)

```bash
make start
```

App sobe normalmente. Mesma UX do pré-cleanup.

## Riscos

| Risco | Mitigação |
|---|---|
| Algum código Go ainda importa do `orchestrator/` via path shadow | `go vet` + `go build` na verificação pós-commit-1 pega. Improvável (F10.5/6/7 verificaram main.go é 100% Go) |
| `tests/` continha E2E validando o binário Go (não só FastAPI) | Pré-execução: `find tests/ -name '*.py' \| xargs grep -l 'build/bin\|jarvis\|Wails'`. **Atenção: false-positives esperados** — string "jarvis" aparece em UIDs de container Docker (`tests/e2e/conftest.py:13,98`), NÃO como referência ao binário Go. Verificar visualmente. Cobertura E2E real hoje vive em `ui/e2e/` via Playwright contra `cmd/jarvis-e2e-http` |
| `Dockerfile.orchestrator` referenciado em algum lugar (CI, docs vivos) | `grep -r 'Dockerfile.orchestrator' . --exclude-dir=docs/superpowers --exclude-dir=docs/adr` deve retornar zero |
| `.dockerignore` é usado por outro Dockerfile remanescente | Não há outros Dockerfiles. Pode deletar com segurança |
| `Makefile.f10` mencionado em onboarding docs (README, etc) | grep + substituir por `Makefile`. Histórico em specs/plans/ADRs NÃO retrofittado |
| User roda `make -f Makefile.f10 ...` por hábito | Erro "No such file" óbvio. Rapidamente reaprende `make ...` |
| ARCHITECTURE.md rewrite descobre buracos ("como ai-jail interage com Wails?") | Trabalho contained (~50 linhas). Se aparecer buraco, parar e perguntar — não inventar |
| Specs/plans antigos em `docs/superpowers/` referenciam código que não existe mais | **Aceito como histórico.** Não retrofittar. ARCHITECTURE/PRODUCT (docs vivos) refletem realidade |

## Dependências externas

Nenhuma adicionada. **Removidas:**

- Python 3.13
- uv (pacote manager)
- FastAPI, SQLAlchemy 2, alembic, pytest, ruff, mypy, testcontainers-python
- `.python-version` constraint

## Entrega

Cada commit isolado faz sentido em revisão:

- Commit 1: revisor confirma deletions são exatamente o que diz (Python).
- Commit 2: revisor confirma rename + ausência de refs em docs vivos.
- Commit 3: revisor lê prosa.

Spec #1 não depende de spec #2 (packaging) — pode mergear sozinho.
